# 2026-09-16

## 技术债清理实施计划（新文档，已入库）

产出两份配套文档：
- `docs/tech-debt-plan-2026-09-16.md` —— 评估模型、分阶段路径、质量门禁、行为保持约束、
  防复发机制、风险登记、10 条完成判据。
- `docs/tech-debt-register.md` —— 台账（16 个债项 + 「刻意保留项」登记 + 待定项）。

**评分模型**：`DPI = (I + P + V + S) / (√C × R)`，I 影响面 / P 恶化速度 / V 可验证性 /
S 战略对齐（各 1–5），C 成本（人天，斐波那契），R 风险系数（低 1.0 / 中 1.4 / 高 2.0）。
用 **√C 而不是 C**：否则「便宜小修」会挤爆 P0，而基础设施级缺口被压到末位。
分档 P0 ≥10 / P1 5–10 / P2 2–5 / P3 <2，另有硬性升档（安全、误导性文档、阻塞他人、
已证明 flaky）与硬性降级（已确认刻意保留）。

**工作量**：41 人天（阶段 0 度量 2.5 / 阶段 1 止血 4 / 阶段 2 门禁 7 / 阶段 3 测试 15 /
阶段 4 架构 10 / 阶段 5 防复发 2.5）。阶段 0–2 共 13.5 人天是高性价比前置。

## 本轮实测发现（可直接引用）

- 根 `ui_manager.ahk` **无任何 `#Include`** 指向它（都指向 `presentation/ui_manager.ahk`）→ 死代码。
- `tests/test_joystick.ahk` 未被 `tests/run_all_tests.ahk` 注册（只注册了
  `test_ahk_executor/test_joystick.ahk`）→ 死文件或从未执行的测试。
- `docs/review/2026-08-20/fix-plan/workspace-snapshot/` 把 5 个生产 AHK 文件快照进 docs（97K）。
- `tools/ahk-bench/_harness.ahk` 与 `tools/ahk-probes/_harness.ahk` 两份 harness。
- JS 23 个文件：**零单测、零 lint、零覆盖率**；E2E 53 用例在 CI 默认关闭。
- coverage 只采集 3 个纯逻辑 crate，**无阈值、job 非阻断**。
- 无 dependabot / renovate，CI 无 `cargo audit` / `npm audit`。
- 无 `clippy.toml`、`#![deny(warnings)]`、eslint。

## 修正：test-map.md 的 K 值漂移

`asd-tauri/docs/test-map.md` 记 `prod_escape` K=2.0，而实际已改为 1.5（我上一轮改了
`baselines.json` 与 `developer-guide.md`，漏了这份）。已修正为
「prod_escape K=1.5/warn 1.25；prod_tick K=1.4/warn 1.2；p50 均为归一化值」。
→ 教训：**K/阈值这类数字散落在多个文档，改一处必须全局搜**。已把「文档-代码一致性」
列为阶段 0 的三检脚本之一。
