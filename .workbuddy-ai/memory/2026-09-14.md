# 2026-09-14 工作日志

## 阶段 8 收尾：精确定刻推广到 sequence / enhanced_*（hybrid）

**结论**：改造完成，四闸门全绿，AHK 全量 642/642 连续 3 轮通过。

### 1. 排查 `Test_ExecutePeriodic_EndToEndLatencyP95Within20ms` 偶发失败

- 现象：完整套件里偶发失败，观测值 **77.70 ms** 与 **28.37 ms**（两次不同），
  单独跑 `SenderPreciseTimingTests` 却 9/9 全绿。
- 排查手法：复制 `run_all_tests.ahk` → 只注册 `SenderPreciseTimingTests` + 换日志文件名，
  跑完删掉。**这是判断「回归 vs 跨测试干扰」最快的一招**。
- 根因：**不是回归**。kpd 默认 15ms、预算 20ms，**可用余量只有 5ms**，
  宿主（OS 抢占）一次 >5ms 延迟就让样本超标。打点后干净环境下 42 个样本全部落在
  15.01~15.02 ms（`min 15.009 / max 15.024`），零离群。

### 2. 真正的缺陷：测试统计口径错了

`_Pct(arr, 0.95)` 索引 = `Ceil(n*0.95)`，**n=7 时索引=7 → 断言的是最大值（P100）**，
宿主抖动一次就红。修正：

| 断言性质 | 统计量 | 样本量 |
|---|---|---|
| 端到端时延 P95 ≤ 20ms | P95 | 42 个（窗口 700ms → 4200ms） |
| 保持时长 == kpd | P95 | 20+ |
| **步进是否漂移** | **中位数** | 20+（漂移是系统性偏移，中位数即可检出，不被个别离群污染） |

另修 hybrid 用例：**旧写法把 `holds`/`steps` 存进 Map，同名键互相覆盖，最终只剩 1 个样本**
（等于单样本断言）。改为数组 + 中位数/P95。

### 3. 新测出的代价：精确定刻会连续占用 AHK 主线程

挂 5ms 探针定时器统计唤醒间隔（interval=100/kpd=15，3s）：

| 指标 | 实测 |
|---|---|
| 探针间隔 P50（定时器网格本身） | 16.08 ms |
| 探针间隔 P95 / 最大 | 43.98 / 47.47 ms |
| **扣除网格后最长连续占用** | **31.39 ms** |
| 占用占空比 | **29.2 %** |

原因：`SleepUntil` 中段用 `DllCall("kernel32\Sleep", 0)` 让出，**不会放行其它 AHK 定时器**
（AHK 定时器靠消息循环派发，回调内不泵消息）。后果：IPC 命令/热键最多被推迟 ~31 ms。
旧实现（`SetTimer(-kpd)` 异步释放）完全不阻塞 —— **这是用单线程忙等换亚毫秒精度的固有代价**。
暂未改（改「多段提前唤醒」可压到 ~16ms，但要多承担一次定时器抖动风险，而时延是首要需求）。

### 4. 隔离探针脚本的坑（下次直接复用）

- `log` 是 AHK v2 内置函数 **`Log()`**，不能作变量名（报 "This Func cannot be used as an output variable"）。
- 脚本级（auto-execute）变量**不会**被箭头函数闭包捕获（函数 assume-local）→ 报
  "This local variable has not been assigned a value"。**把探针主体包进 `Main()`** 即可。
- `while i <= n { ... }` 单行块体非法，必须多行。
- 单独 include `sender.ahk` 时 `Joystick` 未定义 → `_StopGroup` 会抛
  "Item has no value"（line 211）。探针里别调 `ToggleGroup`，或把 `FileAppend` 写在它之前。
- 跑 AHK 探针用 Bash 直接调 `AutoHotkey64.exe`（GUI 程序，PowerShell 不等待）。

### 5. 交付物

- `tests/test_ahk_executor/test_sender.ahk`：4 个时延类用例改为足量样本 + 正确统计量（**未新增用例**，仍 51 个）。
- `docs/perf/key-latency-benchmark-2026-09-13.md`：补 sequence/hybrid 数据、§4.3 同刻多键塌缩、
  §4.4 sequence 首步被跳过、§6 主线程占用实测、§8 测试统计口径踩坑。
- `AGENTS.md`：高精度定刻规范 7 条 + 主线程占用代价 + 时延断言统计口径告警。
- `asd-tauri/docs/test-map.md`：test_sender 13/47→13/51，AHK 260→264，全量 638→642（静态 636→640）。
- 四闸门：图谱 AHK 74/264/0 环（0 新增环）；fmt/clippy 零告警；测试 642/642；文档已同步。

## AHK v2 引擎架构深度调研（源码级 + 实测，报告 + 落地清单）

交付：`docs/research/ahk-engine-architecture-2026-09-14.md`（~1.5 万字）+ `tools/ahk-probes/`（P0–P10 探针 + harness + README）。
**未改任何生产代码**；引擎源码 `AutoHotkey-2.0.26/source` 只读（不修改/不 fork/不编译）。

### 前置校验 P0
两处 `AutoHotkey64.exe` 大小（1 272 832 B）与 SHA256 **完全相同**，`A_AhkVersion` = 2.0.26 = vendored 源码版本
→ 实测对本项目有效。

### 源码走查（T1–T6，6 条轨道）
- 启动链：`_tWinMain` → `LoadFromFile` → `InitForExecution` → `MainExecuteScript`；
  **热键在 auto-exec 之前生效**（`AutoHotkey.cpp:305`），**消息泵在 auto-exec 之后**（`:328`）。
- 解析：1 遍读行 + 3 趟后处理；**`#Include` 是解析期文本展开**（`script.cpp:3542`）并入同一条全局 Line 链表；
  只有「单操作数退化」，无算术折叠、无死代码消除。
- 执行：`Line` 双向链表 + `mJumpToLine` 预解析 → 跳转 **O(1)**；`Var` union + lazy 数字缓存；
  对象**纯引用计数、无 GC、无环检测**（`script_object.h:16-68`）；伪线程 = `g_array[]` 下标 + Line 游标（非 OS 线程）。
- 热键：hook 线程 `PostMessage(AHK_HOOK_HOTKEY)` → 主线程 `MsgSleep` 执行；回调**始终在主线程**。
- 消息循环：`GetMessage()` 阻塞（无 `MsgWaitForMultipleObjects`）；可中断阈值 17ms / 1000 行；
  引擎**主动放弃** `timeBeginPeriod`（`globaldata.h:213-215`）。
- 内存：SimpleHeap = 32KB bump 分配器，**只增不还**，析构从不被调用。

### 探针实测关键结论
- 启动 163.8ms，但本机进程创建基线（`hostname.exe`）就是 **144.9ms** → **引擎自身仅 ≈19ms**。
  解析 1.33µs/行；**`#Include` 592µs/文件**（与行数无关）。
- 15.625ms 网格坐实；**`timeBeginPeriod(1)` 完全无效**（gap 15.985→15.995）；**Sleep 断点在 21ms**（非 16ms）。
- **自旋期间消息泵未停摆**（`Sleep(0)` 驱动泵）：自旋 100ms 期间探针仍触发 7 次；真实代价是 gap p95 16.6→27ms。
- 热键时延 **p50 0.86ms**；**裸 SendInput（extraInfo=0）能触发 IL0/IL1/IL11 全部三档**
  → 坐实「Rust 侧下沉不可行」，`SendLevel(10)+InputLevel(11)` 挡不住它。
- 循环引用 **+22.9MB 全泄漏**（无环对照 +64KB）；1080 次热键注册 **+452KB 不回收**。
- 长路径 **30 038 字符正常**（无 MAX_PATH 限制）；CP936 会**静默丢字符**（长度不变）。
- OnError 返回 -1 继续 / 1 退出线程；**递归 1200 层 OK、1500 层终止且 `try/catch` 捕获不到**；
  **`#MaxThreads` 不丢弃并发定时器**（30/30 全跑）。

### 落地清单
L1 脚本层 9 项 + L2 Rust↔AHK 边界 7 项 + L3 引擎层 7 项（**只评估、明确不实施**）= 共 22 项，
每项含源码依据、代码级片段、优先级、验证方式。

### 踩坑（已固化到 `_harness.ahk` 与 `tools/ahk-probes/README.md`）
`catch as e` / Array 无 `Sort()` / 回调必须传函数对象 / `OnError` 多回调全部执行且移除不掉 /
`StrPut` 数字码页非法 / `SetTimer` 同一函数对象只保留最后一个 / GUI 子系统弹模态框挂死。
