# 2026-09-12 工作日志

## 任务：图谱式全量代码审查（graph-driven code review）

用户请求「审查全部代码，图谱式开发，询问我对齐需求」，经 AskUserQuestion 对齐后确定：
验证 98 个未提交改动 → 先提交形成基线 → 构建依赖图谱 → 基于图谱靶向审查 →
报告 + 全套修复 + 补测试。

## Phase 0：建立干净基线

98 个未提交改动整理为 6 个逻辑提交，工作区完全干净：

| commit | 内容 |
|--------|------|
| `3be543e` | fix(ahk): 落地代码审查修复 — 分层解耦与日志限速 |
| `cfc1aa2` | fix(ahk-executor): 热路径健壮性 — IPC 写失败降级与摇杆/发送器改进 |
| `21fc349` | refactor(rust): 移除死代码 SkillManager 并强化服务层健壮性 |
| `d15f60e` | test: 同步测试套件至重构后接口 — AHK + Rust + E2E |
| `5e97fab` | docs: 归档 2026-08-20 审查全量产物并同步文档 |
| `fdadd20` | ci: 移除已废弃的 test-manifest feature 调用 |

## Phase 1：依赖图谱构建

产出（均为项目产物，已入库）：
- `.review-analysis/build_graph.py` — 图谱构建脚本（可复现）
- `.review-analysis/gen_graph_html.py` — 交付生成器
- `docs/review/2026-09-12/graph/architecture-graph.json` — AI 可读结构化图谱（schema `asd-architecture-graph/v1`）
- `docs/review/2026-09-12/graph/architecture-graph.html` — 交互式图谱（4 视图）

图谱规模：
- AHK：79 文件 / 307 边 / **0 环** / 7 孤点
- Rust：5 crate / 9 生产边 / 2 测试边 / **0 生产环**
- JS：5 文件 / 9 边
- IPC 契约：100% 三端对齐（13 个 `IpcCommand` variant ↔ AHK 分发分支 ↔ Tauri command）
- 分层反向边：仅 1 条 `infrastructure → domain`（3 处，全部在 AGENTS.md 白名单内）

**建图踩坑**（重要，下次直接复用）：
1. AHK `#Include` 提取正则**必须带 `re.M`**，否则 `^` 只匹配字符串开头，边数为 0
2. Rust crate 环检测**必须区分 `[dependencies]` 与 `[dev-dependencies]`**，
   否则 `asd-test-harness` 反向依赖被测 crate 会被误报为循环
3. 分层反向边判定用 **depth 语义 + 豁免层**（`entry`/`tests`/`executor`），
   不能用 order 数值大小比较，否则 `entry → infrastructure` 全部误报

## Phase 2：图谱式审查结论

- Critical 清零
- 历史发现闭合率 **93%**（53/57 已验证项）
- 三轮趋势：2026-08-03 94 项 → 2026-08-20 72 项 → 2026-09-12 16 项（↓78%）
- CR1（ConfigIO/BackupService 半回退崩溃）：实机验证已修复（`OK ConfigIO=Class BackupService=Class`）
- CR2：**降级 Important** —— 热路径已改 `SendInput` 本地调用，IPC 受 `_reportKeyEvents`
  门控（`sender.ahk:34`，默认 `false`），仅录制/验证模式仍同步阻塞

产出：`docs/review/2026-09-12/code-review-report.md` + `issue-inventory.md`

## Phase 3：修复（10 项发现 → 9 项修复 + 1 项技术债登记）

用户经 AskUserQuestion 决策的两项清理：
- `gui.ahk`（2261 行 v1.0 遗留）+ 3 个守护它的失效 I14 测试 → **删除**
- 6 个无法执行的废弃测试（1220 行）→ **移入 `tests/archive/`**

已完成修复：

| 编号 | 内容 |
|------|------|
| A-1 | watchdog unsafe impl 消除（见下） |
| A-2 | `test-map.md` 删除已废弃 `scheduler.rs` 行，重算 186 → 163 |
| A-3 | AGENTS.md 测试统计修正（624 → 实测值） |
| A-4 | 转为技术债登记（见下） |
| B-1 | AGENTS.md 妥协 #1 补入 `config_validator → domain/joystick_input` |
| B-2/B-3 | 6 个废弃测试归档至 `tests/archive/` |
| B-4 | `gui.ahk` 删除 + 3 个失效 I14 测试删除 |
| B-5 | 新建 `command_contract_tests.rs` 命令数量守护（含故障注入验证） |
| B-6 | `migration_logger.ahk` 补齐 10 个测试 |

### A-1 详情（唯一 soundness 相关项，值得记住）

**根因**：`windows 0.62.2` 的 `HANDLE(pub *mut c_void)` **既非 `Send` 也非 `Sync`**
（与 std 的 `OwnedHandle` 不同，std 为其手写了 `unsafe impl`）；
`std::process::Child` 是 `Send + !Sync`。二者直接作为 `ProcessWatchdog` 字段
把 `!Sync` 传染给该类型，原作者因此手写 `unsafe impl` —— 代价是编译器
**从此不再校验任何字段**，新增未保护字段即静默 UB。

**修复方案**：拆成两个可独立审计的命题，收敛到最小泛型边界

```
HANDLE (:: !Send + !Sync)
  └─ RawHandle        : unsafe impl Send           ← 命题 1（句柄是内核对象 ID，可移动）
       └─ SendSyncCell : T: Send ⇒ Send + Sync     ← 命题 2（不暴露 &T，靠外层 Mutex 串行化）
            ├─ JobObjectGuard  : 自动推导
            └─ ProcessWatchdog : 自动推导 Send + Sync + 'static  ← 原 2 处 unsafe 已消除
```

关键设计点：
- `SendSyncCell::with / with_mut` 要求返回值 `R: Send`，从**类型层面阻断借用逃逸**
  （把原作者在 `state()` 上手工维持的不变量升级为编译期检查）
- 手写 `unsafe impl` 由 4 处收敛为 3 处，`ProcessWatchdog` 的 2 处彻底消除
- 新增 `!Sync` 字段：由「静默通过」变为「编译期 E0277」

**两组故障注入均已实测验证**：
1. 注入 `Cell<u32>` 字段 → `E0277: Cell<u32> cannot be shared between threads safely`
2. 加回手写 `unsafe impl Send` → 白名单测试精确报出违规行

新增 3 个回归防线：`test_send_sync_cell_wrapping_chain_is_sound`、
`test_process_watchdog_send_sync_is_derived_not_declared`、
`test_no_handwritten_unsafe_send_sync_for_watchdog_types`

### A-4 技术债登记（明确不实施改造）

录制模式 IPC 同步阻塞写（`ipc_client.ahk:808`，overlapped 传 `Ptr 0`）。
不改造的理由：① 仅录制/验证模式触发（默认关闭）；② 改 OVERLAPPED 需配套重做
管道创建 + OVERLAPPED 结构 + 事件对象 + 写完成轮询 + 缓冲区生命周期，
对 AHK 单线程钩子模型而言相当于重做 IPC 并发模型，收益/风险比不佳；
③ 无实测丢键证据。

**触发重新评估的条件**（满足任一）：
- 出现录制丢键的实测证据
- 录制模式改为默认开启

已在 `ipc_client.ahk` 就地写入上述取舍与触发条件。

## 最终验证状态

| 项 | 结果 |
|----|------|
| `cargo clippy --all-targets` | **0 warning / 0 error** |
| Rust workspace 测试 | **598 passed / 0 failed / 16 ignored** |
| AHK v2 测试 | **616 passed / 0 failed** |
| 测试函数静态计数 | `#[test]` 575 + `#[tokio::test]` 26 = 601 |
| AHK 语法校验 | `main.ahk` / `executor.ahk` / `ipc_client.ahk` 均通过 |

## 提交记录（本日共 8 个）

```
ad8fc9a fix(soundness): 消除 watchdog 的 2 处手写 unsafe impl Send/Sync
0ac8a0b docs(review): 新增 2026-09-12 图谱式代码审查报告与结构化图谱
fdadd20 ci: 移除已废弃的 test-manifest feature 调用
5e97fab docs: 归档 2026-08-20 审查全量产物并同步文档
d15f60e test: 同步测试套件至重构后接口 — AHK + Rust + E2E
21fc349 refactor(rust): 移除死代码 SkillManager 并强化服务层健壮性
cfc1aa2 fix(ahk-executor): 热路径健壮性 — IPC 写失败降级与摇杆/发送器改进
3be543e fix(ahk): 落地代码审查修复 — 分层解耦与日志限速
```

## 待办 / 后续

- `.workbuddy/memory/MEMORY.md` 的入口描述仍提到 `gui.ahk`（已删除），需更新
- 考虑将「图谱式代码审查」流程存为 skill（多步可复用工作流）

---

## 任务（续）：建立图谱式开发流程规范文档

用户请求：梳理项目代码结构/模块依赖/核心链路/关键文件，建立图谱式（节点-关系可视化）
开发流程规范，覆盖六大部分；每阶段主动询问确认。

### 需求对齐（两轮 AskUserQuestion，共 6 项决策）

| 决策项 | 结论 |
|--------|------|
| 规范落点 | AGENTS.md 放摘要 + 指针；完整规范放 `docs/` 独立文件 |
| 链路范围 | 以 v4.0 为主线，v3.0 用注记块标注差异 |
| 图谱形式 | Mermaid 图 + 邻接表 + 脚本链复用说明 |
| 测试数字冲突 | 立「唯一权威」铁律 + 顺手修正 test-map.md |
| 拆分方式 | 拆 3 个文件（主规范 / 邻接表 / 同步清单） |
| scope 白名单 | 仅在新规范说明现状，不改 `commit-convention.md` |

### 交付物

新建 3 份：

| 文件 | 行数 | 内容 |
|------|:----:|------|
| `docs/graph-driven-workflow.md` | 1293 | 主规范 §0-6 + 附录 A/B，含 11 张 mermaid 图 |
| `docs/module-adjacency.md` | 519 | 32 个生产模块的逐文件入边/出边 |
| `docs/graph-sync-checklist.md` | 353 | 可勾选同步清单 + 自检命令 |

修改 3 处：
- `asd-tauri/docs/test-map.md` — 修正 asd-tauri 明细表（5 处改值 + 补 1 行 `command_contract_tests.rs`）、
  汇总 624→601、AHK 607→616、自洽校验行
- `AGENTS.md` — L1313 前新增「图谱式开发流程（摘要）」节（34 行）
- `docs/developer-guide.md` — §1.5 后加指针（避免与图谱规范双份维护）

### 本轮实测修正的既有文档错误

test-map.md 的 asd-tauri 明细表**整段过期**（上一轮 B-5 新增测试未登记）：

| 文件 | 表内 | 实测 |
|------|:----:|:----:|
| `infrastructure/ipc.rs` | 45 | **52** |
| `infrastructure/watchdog.rs` | 29 | **32** |
| `tests/bridge_tests.rs` | 13 | **4** |
| `tests/watchdog_integration_tests.rs` | 17 | **14** |
| `lib.rs` | 7 | **6** |
| `tests/command_contract_tests.rs` | 缺失 | **3**（补行） |

明细 13 行和 = 230 不变，但内部不再自相矛盾；asd-application 186→163；
AHK 607→616（依据 `tests/test_results.log` 2026-09-13 00:57 实跑）。

### 关键技术发现（值得复用）

1. **图谱数据与计划期不同了**：实测 AHK 72 文件 / 254 内范围边 / 9 孤点（计划期记的是
   79 文件 / 307 边 / 7 孤点）——上一轮删除 `gui.ahk` + 归档 6 个废弃测试所致。**写文档前必须重跑脚本**。
2. **`executor.ahk` 的 `case` 计数陷阱**：全文 21 个 `case "..."`，其中 **10 个属模式字符串
   switch**（`periodic`/`sequence`/`enhanced_*`/`hold`/`hybrid`/`joystick_*`，用于
   `MergeModeConfig`），只有 11 个是 IPC action。**校验契约必须用 `sed -n '58,85p' | grep -c` 限定函数范围**，
   用全文件 `grep -c 'case "'` 会误得 21。
3. **Mermaid 校验必须注入 jsdom**：mermaid 在 Node 下校验 flowchart 会调 `DOMPurify.addHook`，
   无 DOM 时报 `DOMPurify.addHook is not a function`——是**环境问题不是语法错误**
   （表现为「时序图 PASS、流程图 FAIL」的迷惑现象）。
4. **Node 把 `/tmp` 解析成 `C:\tmp`**：传给 Node 脚本的路径必须是 Windows 绝对路径。
5. **AGENTS.md 已有 `<!-- MANUAL -->` 标记**（L837）说明存在自动重生成机制，但仓库内无生成脚本；
   故新节放在文档**尾部**（L1313 前）且只放指针不放正文，降低被抹风险。

### 验证结果

| 项 | 结果 |
|----|------|
| 11 张 mermaid 图 | **全部 PASS**（mermaid.parse 官方解析器） |
| test-map.md 自洽 | 132+72+163+3+231 = **601** ✓；明细 13 行和 230 +1 = 231 ✓ |
| 三份新文档测试数字 | **零**（遵守「test-map.md 唯一权威」铁律） |
| 换行符 | 全部 **LF**（含修复被 Edit 工具引入 CRLF 的 developer-guide.md） |
| 文档内路径真实性 | 全部存在（逐条 `[ -e ]` 校验） |
| 代码改动 | **0**（纯文档任务，未触碰任何 `.ahk` / `.rs`） |

### 沉淀的 skill

`~/.workbuddy-ai/skills/mermaid-doc-validate/` —— Markdown 文档 Mermaid 图语法校验工作流
（含 jsdom 注入脚本 + 4 类环境陷阱）。

