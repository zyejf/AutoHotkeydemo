# 测试闸门加固 · 2026-09-19

**执行**：泰莎（Tessa）· 测试专家 | **任务编号** #2 | **状态**：4 项全部完成，闸门最终 rc = **0**

> 全部 cargo 命令均带 `CARGO_INCREMENTAL=0`（rustc 1.95.0 增量编译会 ICE、退出码 101）。
> 未执行任何 git 写操作；改动留在工作树，由主理人统一提交。
> 未改 `docs/tech-debt-register.md`、未改 `scripts/check-tech-debt.py`。

---

## 0. 结论速览

| # | 任务 | 预设 | **实测结论** | 处置 |
|---|------|------|-------------|------|
| 1 | G3h 元闸门 | 「确认有没有类似的守护」 | **部分已有**（C10 管档位漂移，不管判据数值；`--ignored` 计数**无人管**） | 新增 **F1 / F2 两段** |
| 2 | 用例数下限闸门 | 基线 670、容差 0 | 基线当时**已漂到 673**（Cody TD-071 期间 +3） | 新增 **E 段**，下限钉 **673** |
| 3 | 删 `watchdog_integration_tests.rs:14-26` | 「陈旧且与现行判据矛盾」 | **不该整段删** —— 五条全是**前瞻**约束，无一因摘除而失效 | 取 L24 自身允许的**「改写」** |
| 4 | 统一「连续 N 次」判据 | 「三处冲突」 | **实际四处**；且**只有两处是次数口径**，第三处是**速度口径**（不能与次数判据统一） | 2 处统一到 ≥10，1 处重新归类，1 处复述同步 |

**额外发现（不在四项任务内，但同属「闸门自身」）**：CI 的 G3h 步骤**已在静默空转** —— 见 §1.3。

---

## 1. 任务 1：G3h 元闸门

### 1.1 事实核查：现状到底有没有守护？（先查，不臆测）

按「可能已有」的方向逐一打开核实，结果分三类：

| 候选 | 是否已有 | 实证 |
|------|---------|------|
| 「加代码没加测试」的守护 | **已有**（但不是我这条） | G3f 覆盖率棘轮 `scripts/check-coverage.py`；另有 `min_files` 范围锚点。用例总数门**拦不住**「加代码不加测试」（总数不变），只能拦「测试消失」 |
| 「改了判据没同步」的守护 | **部分已有** | `check-tech-debt.py` 的 **C10**：比对 `.sh` / `.ps1` / `ci.yml` 三处**闸门调用档位**（G3d 的 `--no-cargo` 有无、G3i 的 `--selftest` 有无）。它管的是**调用标志**，**不管判据里的数字**（实测：把判据从 ≥10 改成 3-5，C10 全程不红，`check-tech-debt.py` rc=0） |
| 「`#[ignore]` 是否复活」的守护 | **无** | 全仓 `scripts/` 与 `.github/workflows/ci.yml` 检索 `ignored`，除 `ci.yml` 的 G3h 步骤外**零命中**（`grep -rn "ignored" scripts/` 无输出） |

→ 结论：**判据数值**与**排除复活**这两条腿确实无人守，任务 1 的前提在这两点上成立；但「加代码没加测试」那条腿**已有守护**，本轮不重复造。

### 1.2 新增：F1 排除腿 + F2 判据腿（落在 `scripts/check-test-map.py`）

**为什么落在 `check-test-map.py` 而不是新建脚本**：该脚本就是 G3d，已同时被
`scripts/check-gates.sh` / `scripts/check-gates.ps1` / `.github/workflows/ci.yml:256` 三处调用，
且三处都跑**完整模式**（不带 `--no-cargo`）。加在这里 = **零新增接线、零新增 CI 步骤**，
也不会触发 C10（我没有改动任何一处调用行）。

- **F1 排除腿**：`cargo test --workspace --tests -- --ignored --list` 的 `: test` 行数必须为 **0**。
  口径与 B/C/D5 同源（`--tests`），实测与 `--all-targets` 计数一致，故**不额外编译**。
- **F2 判据腿**：三个权威落点里所有「连续/连跑 … N 次」解禁判据，N 必须 **≥ 10**。
  - 只认**同时含解禁语境词**（`#[ignore]` / `ignore` / `解禁` / `摘` / `G3h`）的行，
    实测成功排除无关的 `watchdog_integration_tests.rs:387`（「连续收到 `STABLE_HEARTBEAT_THRESHOLD` 次心跳」）。
  - **锚点**：任一声明落点若**一条都匹配不到 → 硬失败**。判据被删或措辞改掉后本腿会静默失效，
    那正是它自己要防的事。

### 1.3 ⚠️ 额外发现：CI 的 G3h 步骤已在静默空转

```
$ CARGO_INCREMENTAL=0 cargo test -p asd-tauri --lib -- --ignored --test-threads=1 --list
0 tests, 0 benchmarks        ← rc=0
```

`#[ignore]` 于 2026-09-19 摘除后，`--ignored` 筛选**已选不到任何用例**；叠加该步骤的
`continue-on-error: true`，它已从「观测期门禁」退化成**恒绿空转** —— 绿，但一条都没跑过。
这正是 TD-058 定义的 **D 类「通道腐烂 / 假绿」**，且是四项任务之外的盲区。

**处置**：改为按模块名直跑该组用例并保留串行与 `continue-on-error`：

```yaml
run: cargo test -p asd-tauri --lib tests::watchdog_integration_tests -- --test-threads=1
```

- 实测该筛选命中 **17 条**（`--list` 计数实证，不再是 0）。
- `continue-on-error` **维持不变** → 阻断行为与风险敞口**完全没有变化**，不引入新的红源；
  只是把「0 条」变回「17 条」。串行（`--test-threads=1`）是为采集抖动证据，不是为了阻断。
- ⚠️ 这是**行为性变更**，虽风险为零，仍建议由 archi / rex 在发布链路评审时过一眼。

---

## 2. 任务 2：用例数下限闸门（E 段）

### 2.1 基线实测（口径即判据，带时刻）

| 命令 | 团队给的基线 | **本机实测（2026-09-19 22:29）** |
|------|------------|--------------------------|
| `cargo test --workspace --all-targets -- --list` | 670 | **673** |
| `cargo test --workspace -- --list`（不带 `--all-targets`） | 667 | **674** |
| `cargo test --workspace --lib -- --list` | 483 | **490** |
| `cargo test --workspace --tests -- --list` | — | **673**（与 `--all-targets` 一致） |
| `cargo test --workspace --tests -- --ignored --list` | 0 | **0** |

⚠️ 两点必须留痕：
1. **基线是一个移动靶**。实测轨迹：21:53 = 670 → 22:08 = **673** → 22:16 = **674** → 22:29 = **673**。
   漂移全部来自 Cody 的 TD-071（`src-tauri/src/infrastructure/ipc.rs` 54 → 57 → 58 → 57）。
   **任何写死的基线数字都必须带时刻与命令**。
2. 「不带 `--all-targets` 是 667」与实测 674 的差，是**快照时点差**（不是口径错）：
   673 与 674 差 1，且这个 1 条差异**两次都稳定复现**（670/671、673/674），**未做根因分析**。
   本闸门统一用 `--tests` 口径（与 B/C/D5 同源）。

### 2.2 下限取值：钉 **673**，不是 670

取**实测水位 673** 而不是沿用给定的 670。理由：下限钉的是**当前水位**，沿用 670 等于默许
「先加 3 条、再删 3 条」而闸门不响。容差 **0**、只许增不许减。抬升须显式改常量并说明理由，
沿革写在常量注释里（670 → 673）。

### 2.3 它拦得住什么、拦不住什么（不夸大）

「测试静默消失」有五条路径，**只有第 ① 条会被 F1 抓到，其余四条只有 E 能抓**：

| # | 路径 | F1（0 ignored） | E（总数下限） |
|---|------|:---:|:---:|
| ① | 挂 `#[ignore]` | ✅ | ✅ |
| ② | `#[cfg]` 排除出编译 / 整段注释 | ❌ | ✅ |
| ③ | 文件在但没被 `mod` 挂进编译 | ❌ | ✅ |
| ④ | 测试文件被整个删除 | ❌ | ✅ |
| ⑤ | `Cargo.toml` 的 target 配置被改（`test = false`） | ❌ | ✅ |

本仓有 ④ 的真实先例：`bridge_tests.rs` 的 `test_tauri_event_bridge_emit` 是**整个被删除**而非挂 ignore。

**拦不住**：「加了生产代码但没加测试」—— 总数不变，E 不会响。这条腿由 **G3f 覆盖率棘轮**守（已有），
本轮不重复造，也不声称已覆盖。

---

## 3. 任务 3：`watchdog_integration_tests.rs:14-26` —— **结论：不该整段删**

### 3.1 逐条核实（读完再判断，不顺从预设）

| 行 | 内容 | 是否因「已摘 `#[ignore]`」而失效 |
|----|------|:---:|
| L14 | 「**将来**若有人要为『真实外部进程』类用例新挂 `#[ignore]`，必须先满足下列解禁判据」 | ❌ 前瞻，未失效 |
| L15-17 | 裁决理由：真实进程用例有真实失败面，摘了是用一种债换另一种债 | ❌ 未失效 |
| L18-19 | ① 隔离（temp_dir + 唯一子目录，跑完 kill + 删目录） | ❌ 未失效 |
| L20 | ② 串行（`--test-threads=1` 下先证明稳定） | ❌ 未失效 |
| L21-22 | ③ 外部依赖可自检（拿不到须显式打印「跳过」并 return） | ❌ 未失效 |
| L23-24 | ④ 本机与 CI 各连续 ≥10 次全绿才可摘 | ❌ 未失效（但与 ci.yml 的 3-5 冲突 → §4） |
| L25-26 | ⑤ 替身优先 | ❌ 未失效 |

**五条无一因摘除而过期**。整段删掉 = 全仓**再没有任何成文约束**阻止「真实进程用例被随手挂回 `#[ignore]`」，
等于用一个新缺口换掉一个自指条款。这是净损失。

### 3.2 真正陈旧的是哪一句

只有 **L24 的末半句**：「摘的同时必须把本段判据一并删除或改写，不许留下过期说明」。
它未被遵守（摘了，段还在）→ 从**该文件自己的标准**看，这整段就是它禁止的「过期说明」。
但这句话是**自指的**：照字面执行（删除）会毁掉本段仍在生效的前瞻守护。
即：**错的是这条规则自身，不是规则所保护的内容。**

### 3.3 处置（取 L24 自己给出的另一个选项：「**或改写**」）

1. 删掉自指那半句，改为「摘的同时必须把本段**改写为『已达成』留痕**」——既满足「不许留下过期说明」，
   又不自毁守护。
2. 显式写明**本段是前瞻约束、不得整段删除**及理由，防止下一个人照字面再删一次。
3. 在 L5-8 上方加一句，把「约 13 秒 / 15 通过」**重新归类为「被证伪的旧排除理由的反证」**，
   并声明「解禁判据只有第 4 条一个、是次数口径」—— 见 §4.2，这是三套口径被混用的根源。
4. 判据 ⑤（替身优先）**本轮未完成评估**，按事实留痕说明（不替人拍板，也不假装已完成）。

---

## 4. 任务 4：统一冲突判据 —— **实际四处，不是三处**

### 4.1 枚举结果（含行号，逐行打开核实）

| # | 落点 | 原文口径 | 性质 |
|---|------|---------|------|
| ① | `.github/workflows/ci.yml:164` | 连续跑绿 **3-5 次** | 次数口径 |
| ② | `watchdog_integration_tests.rs:23-24` | 本机与 CI 各连续 **≥10 次** | 次数口径（最严） |
| ③ | `watchdog_integration_tests.rs:5-8` | 实测约 **13 秒**且 **15/15 通过** | ⚠️ **速度口径，不是次数判据** |
| ④ | `docs/guard-effectiveness-checklist.md:45` 与 `:58` | 连续绿 **3-5 次** | 次数口径（文档复述） |

**与预设的两点偏差**：

1. **是四处，不是三处** —— `docs/guard-effectiveness-checklist.md` 的 `:45`、`:58` 是第四处，
   且同样写 3-5。漏了它，统一就是不完整的（而它恰恰是「闸门有效性清单」，被人当权威引用）。
2. **其中一处根本不是次数判据** —— ③ 是「不慢」，是**被证伪的旧排除理由的反证**。
   「快」不等于「不 flaky」，两者**不可互相统一**；把它当第三套次数判据去「统一到 ≥10」是范畴错误。
   正确处置是**重新归类并显式标注**，让后人无法再援引它当解禁依据。

### 4.2 处置

- ① `ci.yml` → 改「本机与 CI 各连续 **≥10 次全绿**（含 `--test-threads=1`）」。
- ② 原文 ≥10，保持；补注「三处已统一为最严口径」+「由 `check-test-map.py` 的 F2 段机器校验」。
- ③ 加注：明确它**不是解禁判据**，「两者不可互相替代：快不等于不 flaky」。
- ④ 两处同步改为 ≥10，并注明由 F2 段校验。
- **机器化**：F2 段把「N ≥ 10」钉成硬判据，三个落点任一处改小即红、任一处判据消失（锚点）即红。

---

## 5. 阳性对照（每组都实测，非推断）

| 组 | 注入的缺陷 | 期望 | **实测** |
|---|-----------|------|---------|
| **F2-1** | 不改动，**用当前未统一的现实状态**跑 | 点名 3 处 3-5 | ✅ rc=1，精确点名 `ci.yml:164` + `guard-effectiveness-checklist.md:45`、`:58`；`watchdog_integration_tests.rs` 的 ≥10 **正确放行**；心跳判据 `:387` **正确排除** |
| **F2-2** | 统一改为 ≥10 后 | rc=0 | ✅ rc=0 |
| **F2-3** | 删掉 `ci.yml` 的解禁判据行（测锚点） | 静默失效必须红 | ✅ rc=1：「未匹配到任何『连续 N 次』解禁判据 —— 判据被删了还是措辞改了？」；还原后 rc=0 |
| **F1** | 在 `asd-domain/src/models.rs` 的 `test_skill_group_from_group_config` 上加一条 `#[ignore]` | 只有 F1 红 | ✅ rc=1：「全仓 `#[ignore]` 运行时注册数 **1** ≠ 0」，**并点名到具体用例**；B/C/D5/E 全部不受影响（证明是独立一条腿，不是靠总数变化间接发现）。已还原，`git diff` 该文件的 diff 为空 |
| **E** | 把下限临时抬到 674（实测 673） | E 红 | ✅ rc=1：「Rust 测试总数 673 < 下限 674」，附各 crate 明细（asd-domain=161 / ipc=72 / application=177 / harness=3 / tauri=260）。已还原为 673 |

---

## 6. 改动清单与最终状态

| 文件 | 改动 |
|------|------|
| `scripts/check-test-map.py` | **新增 E 段**（`MIN_TOTAL_TESTS = 673` + 水位沿革注释）、**F1 段**（`count_ignored_tests`）、**F2 段**（`check_f_meta_gate` + 锚点）；main 增加打印行；顶部 docstring 同步 |
| `asd-tauri/docs/test-map.md` | `infrastructure/ipc.rs` 54→**57**（含 TD-071 三条用例说明）；asd-tauri 小计 256→**259**、总计 257→**260**；汇总行 670→**673**；自洽校验行 257→**260**、670→**673**；`--lib` 注释 256/257→259/260；新增 E 段说明 |
| `.github/workflows/ci.yml` | G3h 步骤：`--ignored` → 按模块名直跑（`tests::watchdog_integration_tests`，实测 17 条）；注释重写（空转实证 + 判据统一到 ≥10） |
| `asd-tauri/src-tauri/src/tests/watchdog_integration_tests.rs` | L5-8 速度口径**重新归类**；L23-26 判据 4 **改写**（删自指删除句、加「不得整段删除」理由、加三处统一与 F2 校验说明）；判据 5 补「替身优先评估本轮未做」留痕 |
| `docs/guard-effectiveness-checklist.md` | `:45`、`:58` 两处 3-5 → ≥10，并补 G3h 空转实证 |

**回归验证**：

```
CARGO_INCREMENTAL=0 python scripts/check-test-map.py   → FINAL_RC = 0
[A] 通过  [D] 通过  [F2] 通过  [B] 5/5 OK  [D5] 通过
[E] 通过（实测 673）  [F1] 通过（0 ignored）  [C] 登记 673 / 实际 673
[PASS] 闸门③ 通过

cargo fmt --all --check                                 → rc = 0
python scripts/check-tech-debt.py                       → rc = 0（C3b 新增 0 项；C13 核对 88 行通过）
python -c "yaml.safe_load(ci.yml)"                      → YAML 合法，G3h 步骤字段正确
```

---

## 7. 需人拍板 / 遗留（本轮未做，不假装完成）

1. **≥10 次的留痕仍未补齐**。三套口径已统一，但「本机与 CI 各连续 ≥10 次全绿」的**实际跑次记录仍然只有
   1 次本地 + 1 次 CI**（2026-09-18 的旧记录）。统一口径只是让「是否已满足」变得**可判定**，
   不等于**已满足**。→ 建议：要么真跑满 10 次并留痕，要么**显式决议下调判据并三处同改**
   （下调会被 F2 拦住，正是设计意图：逼人显式改，不许悄悄改）。
2. **判据 ⑤「替身优先」未完成评估**。已在源码注释留痕，指向本报告 §3.3 第 4 点；
   是否做替身、或显式接受真实进程用例进 CI，需人拍板。
3. **`continue-on-error` 的摘除时点**仍无到期日。已按 §1.3 保留观测期语义；转硬门禁的触发条件
   现在是「≥10 次留痕」，缺的是**日期**。
4. **E 段下限的抬升仍靠人自觉**。与 G3f 的 `min_files` 同构问题：若有人一边加测试一边抬常量，
   闸门不会响。本轮未做「常量抬升必须伴随明细行增加」的交叉校验。
5. **`--all-targets`(673) 与不带该参数(674) 稳定差 1 条**，未做根因分析。E 段统一用 `--tests` 口径，
   不受影响，但引用这两个数字时**必须写明命令**。
6. **`docs/tech-debt-register.md:130` 与 `deliverables/…/production-readiness-2026-09-19.md`** 里仍写着
   旧的「3-5 次」与「三套」。前者归主理人（我未改），后者是当日历史报告快照（建议不改，避免篡改历史）。
