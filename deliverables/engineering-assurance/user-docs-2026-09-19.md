# 用户侧文档三件套交付索引 · 2026-09-19

**作者**：docu（多库 / Technical Writer）  **任务编号**：#3（阶段 2 用户侧文档）  **状态**：落地完成，4 个文件已落盘

---

## 🔴 0. 入库状态（**最重要，先读这段**）

**截至 2026-09-19，本报告的 4 个文件全部 `tracked = NO`** —— Rex 在 Go/No-Go 核实（`git ls-files --error-unmatch`）中确认，记为 **N4**，并据此判「对外分发 No-Go」。

**这意味着：此刻触发发布构建，产出的 NSIS 包不含 README、不含用户手册、不含排障表 —— B8/B9/B10/B11 四个阻断在包里一个都没解除。** 发布候选 commit `9fd8a69` 与 CI run `35446746668` 均不含这些文件。

**入库由主理人统一执行。** 我受硬性约束「禁止任何 git 写操作」（commit / push / reset / update-ref / stash / `git rm`），**不会也不应自行提交**。

建议主理人按路径**显式指定**提交，勿用 `git add .`（工作树里还有 Cody 的 TD-071 半成品改动：`ipc.rs` / `Cargo.toml` / `Cargo.lock`，状态 M 未提交）：

```bash
git add README.md docs/user-guide.md docs/troubleshooting.md \
        docs/developer-guide.md \
        deliverables/engineering-assurance/user-docs-2026-09-19.md
```

（第 4 行 `docs/developer-guide.md` 是本次新增的 §0 修复，详见 §5.1。）

---

## 1. 交付物

| # | 路径 | 性质 | 主要阻断项覆盖 |
|---|---|---|---|
| 1 | `README.md`（仓库根） | 新建 | **B8**（无 README） |
| 2 | `docs/user-guide.md` | 新建 | **B9**（全仓零用户手册） |
| 3 | `docs/troubleshooting.md` | 新建 | **B10**（唯一排障表要求调内部 API）、**B11**（安装前置与文档矛盾） |
| 4 | `deliverables/engineering-assurance/user-docs-2026-09-19.md` | 本报告 | 索引、引用关系、不确定项登记 |

---

## 2. B8–B11 → 文档节映射

来源：`deliverables/engineering-assurance/production-readiness-2026-09-19.md` §2.3

| 阻断项 | 一句话描述（报告原文缩） | 对应文档 | 对应章节 |
|---|---|---|---|
| **B8** | 无 README | `README.md` | 全文（项目定位、5 分钟上手、文档索引、许可、前置要求） |
| **B9** | 全仓零用户手册 | `docs/user-guide.md` | 全部 10 节 |
| **B10** | 唯一排障表要求用户调内部 API | `docs/troubleshooting.md` | §5 速查表把「开发文档里的 `invoke(get_executor_status)` / `invoke(reset_watchdog)`」明确改为「终端用户用『诊断信息』页」，并在 §2.4 给出 GUI 路径 |
| **B11** | 安装前置未说明，文档与 `tauri.conf.json` 矛盾 | `README.md` §「5 分钟上手」第 3 步 + `docs/user-guide.md` §1.1 + `docs/troubleshooting.md` §1.1、§1 表格 | 三处一致说明 WebView2 是 Tauri 在 Windows 上的渲染依赖；`tauri.conf.json` 的 `webviewInstallMode = downloadBootstrapper + silent` 真实配置**已在 troubleshooting §1.1 揭示** |

---

## 3. 引用的截图（均已逐一 Read 核验）

`func_test/` 下 16 张截图全部阅过；引用关系如下。

| 截图 | 实际页面/状态 | 引用文档 |
|---|---|---|
| `01_dashboard_initial.png` | 仪表盘初始（2 分组全停） | README §5 分钟上手、user-guide §2 |
| `02_group_running.png` | 仪表盘 · 1 分组运行中（hybrid 模式） | README §5 分钟上手 |
| `03_settings_page.png` | 全局设置 · 暗色 · 默认控制热键 | （未直接引用；与 10/11 重复） |
| `04_debug_panel.png` | 调试监控 · 实时日志 + 性能面板 | （未引用，user-guide §2.1 仅提到此页存在） |
| `05_backup_page.png` | 备份管理 · 备份列表 | user-guide §8.1 |
| `06_keytest_page.png` | 按键测试 · 录制模式 · 周期导出 | user-guide §5 |
| `07_dashboard_final.png` | 仪表盘 · 与 01 几乎一致 | （未引用，与 01 重复） |
| `08_dashboard_multiselect_result.png` | 仪表盘 · 2 分组都运行中 | user-guide §3.3 |
| `09_group_editor.png` | 分组编辑 · 5 个快捷模板、基本信息、执行模式 | user-guide §3.1 |
| `10_settings_before.png` | 全局设置 · 切换前 | （未直接引用） |
| `11_settings_dark.png` | 全局设置 · 亮色主题 | （未直接引用；user-guide §6.2 用 12 替代） |
| `12_settings_hotkey_change.png` | 全局设置 · 紧急停止热键输入框捕获态 | README §5 分钟上手、user-guide §6.1 |
| `13_debug_panel.png` | 调试监控 · 自动滚动已关闭 toast | troubleshooting §5 速查表 |
| `14_backup_created.png` | 备份管理 · 创建备份后的 toast | user-guide §8.1 |
| `15_keytest_verify.png` | 按键测试 · 验证模式 | user-guide §5.1 |
| `16_final_dashboard.png` | 仪表盘 · 与 07 几乎一致 | （未引用，与 07/01 重复） |

**全部相对路径以仓库根为基准**（如 `./func_test/01_dashboard_initial.png`），与仓库实际结构一致 —— 已 ls 核对。

> ⚠️ **截图集遗漏的页面**：「诊断信息」页（index.html 第 40 行 `data-page="diagnostics"` + main.js line 469 有标题）**不在 `func_test/01..16` 之内**。这是排障的关键入口（重置看门狗按钮就在这里），但**没有可用截图可引**。已在 troubleshooting §2.4 / §5 用文字明确描述路径与字段，未伪造截图。

---

## 4. 已核实源码的事项（可放心引用）

下列结论都通过直接读源码 / 配置文件确认，不是臆测：

| 事项 | 证据 |
|---|---|
| `tauri.conf.json` 的 WebView2 配置 = `downloadBootstrapper + silent` | `asd-tauri/src-tauri/tauri.conf.json:52-55` |
| 项目许可为 GPL-2.0-only（与 `bundle.license` 一致） | `asd-tauri/src-tauri/tauri.conf.json:36` `license: "GPL-2.0-only"` + 根 `LICENSE` 是 GPLv2 全文 |
| `LICENSE` 是 GPLv2 全文（非 SPDX 短文本） | `head -5 LICENSE` 第 1 行 `GNU GENERAL PUBLIC LICENSE / Version 2, June 1991` |
| 日志路径 = `%APPDATA%\com.asd.tauri\asd.<YYYY-MM-DD>.log`，每天轮转，保留 7 天 | `asd-tauri/src-tauri/src/infrastructure/logging.rs:5-50`（含 `Rotation::DAILY` + `filename_prefix("asd")` + `max_log_files(7)`） |
| 配置路径 = `app_data_dir/config.json`；备份目录 = `app_data_dir/backups/` | `asd-tauri/src-tauri/src/lib.rs:710-715` `app.path().app_data_dir().join("config.json")` + `commands/config_cmd.rs:158-174` 的 `backup_service::export_config/import_config` 走同目录 |
| 7 个导航页面 + 标题 | `asd-tauri/index.html:19-40`（7 个 `data-page`）+ `src/main.js:469`（titles 字典） |
| 10 种执行模式 | `src/main.js:215-220` `MODE_NAMES`（periodic / sequence / hybrid / hold / enhanced_periodic / enhanced_sequence / enhanced_hybrid / joystick_periodic / joystick_sequence / joystick_hold） |
| 5 个快捷模板按钮（不含手柄与增强混合） | `asd-tauri/index.html:109-113`（5 个 `data-action="template*"`） |
| 控制热键 5 项及其默认值 | `asd-tauri/index.html:174-225` 区域 + screenshot `03` |
| `resetWatchdog` / `getExecutorStatus` 都有 UI 调用入口 | `src/main.js:843-849` `resetWatchdogDiag` + `src/main.js:1423` `data-action="resetWatchdog"`；`src/api.js:38-43` 都有 export |
| 紧急停止、全局开关、热重载三个按钮的 always-visible 位置 | `asd-tauri/index.html` 顶部 toolbar + screenshot 01–16 一致 |
| B6（配置加载失败）有 UI 弹窗（不是只写日志） | `src/main.js:811-833` `checkConfigLoadFailure()` + `confirmDialog` |
| 录制与验证互斥 | `asd-tauri/src-tauri/src/commands/recording_cmd.rs:34-39` 文档注释明示（已在 user-guide §5 体现） |
| `ipc:listener-failed` 事件提示跳备份页 | `src/main.js:830-836`（troubleshooting §2.1 第 1 步有引用） |

---

## 5. 不确定 / 待人工确认的事项

### 5.1 ✅ 已修：`docs/developer-guide.md:700`（Rex 于 Go/No-Go 中提出）

Rex 指出存量 `developer-guide.md:700` 仍写「Windows 10+ 内置」，与 `tauri.conf.json` 的 `downloadBootstrapper` 矛盾，且查不到修痕。**我已核实并修复**。

**核实结论（`docs/developer-guide.md` 的三处存量缺陷，逐一 Read 确认）**：

| 位置 | 缺陷 | 当前状态 |
|---|---|---|
| `:700` WebView2 行 | 写「Windows 10+ 内置」，与 `tauri.conf.json:52-55` 矛盾 | ❌ **原本未修 → 本次已修**（见下） |
| `:1601` 代码围栏 | 报告称第 105 个围栏（奇数＝开）吞掉 1603–1633 整节 | ✅ **已修**（1601 开、1603 闭，`### 4.6.6` 在 1605 正常渲染） |
| `:1679-1686` §4.8 | 报告称仍写 `asd.log` + `Roaming\asd-tauri\` | ✅ **已修**（现为 `com.asd.tauri` + `asd.<YYYY-MM-DD>.log`，与 `infrastructure/logging.rs:28-33` 实测一致） |

**已应用的修改**（`developer-guide.md:700`，单行）：

- 改前：`| WebView2 | Edge Chromium 内核 | Windows 10+ 内置 |`
- 改后：明确写出「并非 Windows 10 必然内置」，列出 LTSC / Server / 精简镜像 / 卸载过 Edge 四种例外，点明 `downloadBootstrapper + silent` 的真实行为与「离线必须预装」的后果，并交叉链接到 `troubleshooting.md` §1.1。

⚠️ 这是个 **M 状态**（`developer-guide.md` 已 tracked），**同样需要主理人显式提交**才生效 —— 见 §0 的 `git add` 建议。

### 5.2 仍待人工确认

下列项目**我在文档里有意写得偏保守**或**留了口子**，请人工复核：

| 项 | 我做了什么 | 为什么 |
|---|---|---|
| 应用图标文件名 `ASD.exe` 与安装目录 `%LOCALAPPDATA%\Programs\ASD - 技能管理器\` | 文档照实写了；来自 `tauri.conf.json` 的 `productName` + Windows 默认安装路径推断 | 未在文件资源管理器实测确认安装后的真实路径与图标名 |
| 安装包历史「最长 90 天有效」的说法（README §5 分钟上手） | 引自 task 简报里 Rex 的背景介绍 | 未在 Releases 页实测，**建议 Rex 复核措辞**，避免与实际 CI 配置不符 |
| 安装器类型（MSI / NSIS 各自何时出现） | troubleshooting §1.2 按经验分两类写 | 仓库只有一个 CI workflow，**实际产物以 CI 跑出来为准**，未逐文件实测 |
| Joystick 三种模式在 UI 中的选择路径（user-guide §4） | 写了「手柄模式需要在执行模式区手动选择」 | 编辑页截图 09 看不到手柄模式的切换按钮，但代码确认模式存在；**实际 UI 入口是否藏在「执行模式」折叠区需要二次确认** —— 标注 ⚠️ 已提示 |
| AHK 修饰键的语法（`^` = Ctrl, `!` = Alt, `+` = Shift） | user-guide §5 提到，没在 AHK 官方文档二次核实 | 这是 AHK v2 通用语法，但写进「用户文档」前建议 Tessa 复核一遍措辞 |
| 备份文件命名格式 `backup_YYYY-MM-DD_HH-MM-SS[_xxx].json` | 文档里用通配格式 | 实际后缀规则（`_auto` / `_pre_reload` 等）只在截图 05/14 里看到几种，**可能还有其他后缀**（如 `_manual`），但不重要 —— 用户不需要知道 |
| 状态栏底部圆点「就绪 / 启动中 / 失败」对应颜色 | user-guide §2.3 与 troubleshooting §2.4 都写了 | 颜色是从截图肉眼判断的（绿 / 黄 / 红），**未读 CSS 变量二次确认** |

---

## 6. 未触碰的硬约束遵守情况

| 约束 | 状态 |
|---|---|
| 禁止任何 git 写操作 | ✅ 0 次 commit / push / reset / `git rm` |
| 不碰 `asd-tauri/docs/test-map.md` | ✅ 未读、未改 |
| 不碰 `docs/tech-debt-register.md` | ✅ 未读、未改 |
| 不递归 `asd-tauri/target/` | ✅ 所有搜索加 `--include="*.rs"` / glob 限制 |
| 不批量转 LF/CRLF | ✅ 用 Write 直接写，文件保持当前工作树状态 |
| 文档不写死测试数字 | ✅ README / user-guide / troubleshooting 均无具体测试数；如需指针指向 `asd-tauri/docs/test-map.md` |
| 许可说明写对 GPL-2.0-only | ✅ README 专设「许可」一节 |

---

## 7. 与上下游的对接

- **Task #1 TD-071（DACL）**：troubleshooting §1 提到「双击安装包后什么也没发生」，**DACL 修复完成前可能与 IPC 监听失败表现混淆**；建议 Rex 修完后做一次端到端复测，确认 §2 表格里的 `ipc:listener-failed` 跳转仍是有效路径。
- **Task #2 测试闸门**：本文未引用任何具体测试数，与 G3h 元门禁不冲突。
- **Task #4 发布链路**：README §「许可」明确 GPL-2.0-only，发布到 GitHub Release 时 license 元数据直接读此条目即可。
- **Task #5 CI 结论核实**：troubleshooting §2.4 §6 提及「日志文件位置」，与 Tessa 维护的日志路径规范一致。

---

## 8. 自我复核清单

- [x] 3 份文档 + 1 份索引报告全部落盘
- [x] 路径引用全部基于真实存在的文件
- [x] 截图引用全部基于 `Read` 看过的实际内容
- [x] UI 行为描述全部基于源码 `grep` / `sed` / `Read` 核实
- [x] 不写测试数字
- [x] 不写死许可证版本
- [x] `developer-guide.md:700` 矛盾已核实并修复（§5.1）
- [x] 无 emoji（按系统默认）
- [ ] ❌ **入库 —— 未做，且按硬性约束不做**（见 §0）。4 个文件 `tracked = NO`，**在包里不生效**，需主理人显式 `git add` 指定路径后提交。这是本报告唯一未完成项，也是 Rex N4 / 对外分发 No-Go 的直接原因。
- [ ] ⚠️ 7 项内容不确定项已诚实标注（§5.2），未隐藏、未编造，但**尚未经人工复核**