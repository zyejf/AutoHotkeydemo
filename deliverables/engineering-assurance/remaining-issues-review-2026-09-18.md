# 剩余问题审查报告：ASD 技能管理器技术债收口后

**日期**：2026-09-18
**工作流**：工作流 5（技术债评估）+ 工作流 4（发布前检查）混合
**参与成员**：Cody（代码审查师）· Archi（系统架构师）· Rex（SRE）· Tessa（测试专家）· Docu（技术文档师）

---

## 📌 TL;DR

- 上轮技术债收口（TD-049~TD-057）**确实是干净的**，工作区 0 改动、门禁 G1~G3g 全绿。本轮审查的是**收口之后还剩下什么**。
- **没有发现新的阻塞项**。剩余 12 项全部是 P1/P2 级，且其中 4 项已经是「做与不做都行」的判断问题，不是缺陷。⚠️ **台账 DPI 全量重算后 9 行档位升到 P0**（TD-018/034/036/037/040/056/057），均为**已完成状态**、不影响排期，见 §1.4。
- ⚠️ **最大的问题不在代码，在台账**：`docs/tech-debt-register.md` 作为「唯一登记处」**自身失去自洽** —— 统计行写 49 项实际 57 项；TD-037 实际已闭环却仍标"待排期"；TD-007 的技术前提**是错的**（不需要 Windows runner）。
- ✅ **TD-044 已闭环**（审查时曾判为 🔴 真缺陷 EBW-4）：原判据「EBW-4 = Tauri 验证 100% 失败」**已被证伪**（三人独立核对三处引用，全错）。落地动作：删除 `index.html` 的 6 个死节点 + 摘掉 `main.js:1097` 活引用；AHK 侧 `app_ui.html` 的 6 个同名节点**全是活代码，未动**。⚠️ **功能缺口仍在，但性质是「前端没渲染」而非「后端没实现」** —— 后端 `start_validation` 完整实现（`recording_cmd.rs:140` → `recording_service.rs:465`，73 行、3 处 `compare_exchange`、5s 超时 IPC），缺的是渲染到报告面板的前端代码（删除 ≠ 接上渲染）。详见 §1.1（原始分析过程保留在 §3.2）。
- ⏳ **E2E 通道的三个死因已修复、修复已上库（`10b57d7`），但**尚未真实验证**（审查时判为 🟠「套件活、通道死」）：三个死因（下载源被微软停用 → 换新域且须给完整四段版本号；`AutoHotkey64.exe` 被 `*.exe` 排除致 `build.rs` 必红 → 补 AHK 前置；`tauri-driver` 无预编译 → 加缓存）均有 `curl` / `git check-ignore` 实测依据。已于 2026-09-18 手动触发 run **`35309066359`**，**结果待出**。🚨 此前记的「45 通过 / 8 失败」等执行结果数字**无来源，已全部撤除**。详见 §1.2。
- 严重度分布：🔴严重 1 项 / 🟠高 4 项 / 🟡中 4 项 / 🟢低 3 项 —— **审查当时快照**；其中唯一 🔴 项（EBW-4）判据已证伪（§1.1），🟠 中的 E2E 通道**死因已修复、执行结果待验证**（§1.2）。
- ✅ **已落地（本轮收尾）**：15 条 watchdog 进程生命周期用例已接入 —— 本地实测 **15 passed / 0 failed / 13.39s**，CI（`ci.yml` gates job 步骤 14 `G3h`）实测 **success**。它们的排除理由「避免在 CI 中运行慢测试」经实测**证伪**（13 秒、全绿）。观测期 `continue-on-error` 不阻断，跑绿 3-5 次后摘 `#[ignore]` 转硬门禁。
- ✅ **本轮已落地（详见 §一）**：① **TD-044 已闭环**（删 6 死节点 + 摘 `main.js:1097`；AHK 侧同名节点活代码未动；EBW-4 判据**已证伪**；**功能缺口仍在**）；② **E2E 通道三个死因已修复并上库**（commit `10b57d7`，**执行结果待验证** —— 已手动触发 run `35309066359`）；③ **守护有效性清单** `docs/guard-effectiveness-checklist.md` 建立（代价排序：**假阳性 > 静默失效 > 通道腐烂**）；④ **台账 DPI 全量重算**（58 行中 **12 行对不上**，TD-056 声明 3.0 vs 公式 **10.0**）+ **C9 校验**落地。

---

## 🎯 核心结论卡片

| 项目 | 内容 |
|------|------|
| 整体评级 | 🟡 有条件通过（发布需先补前提） |
| 阻塞项数量 | 0（2 个前提中：✅ 记忆目录已处置 `20c5537`；⏳ G4 人工确认待完成） |
| 关键行动项 | **6 条**（① 部分落地、② 已落地、③⑥ 等 E2E 真实结论，见行动清单） |
| 建议下一步 | 台账 DPI 重算 + C9 已落地，**统计行 / 状态词 / 快照数字仍待补**；E2E 等 run `35309066359` 的真实结论（§1.2） |

---

## 一、本轮已落地（2026-09-18 下午执行结果）

> 本节记录本轮审查**之后已实际执行完**的动作，与后文「审查结论 + 待决策」区分。
> ⚠️ **后文各节的定性若与本节冲突，一律以本节为准** —— 后文保留的是审查当时的判断过程，供追溯，不描述当前状态。

### 1.1 TD-044 已闭环（原列为待决策）

- 删除 `asd-tauri/index.html` 的 **6 个死节点**：`valReport` / `radarCanvas` / `deviationCanvas` / `intervalCanvas` / `holdCanvas` / `valHistoryList`
- 同步摘掉唯一活引用 `asd-tauri/src/main.js:1097` 中操作 `valReport` 的那半句（**保留 `valStatus` 设色**）—— 不删会 `null.style` TypeError
- ⚠️ **AHK 侧 `presentation/app_ui.html` 的 6 个同名节点全是活代码，未动**（`showValReport` 定义 3402 / 调用 3372；`renderValHistory` 定义 3473 / 调用 3407；四个 canvas 均被 `MiniChart` 调用）
- ⚠️ **原判据「EBW-4 = Tauri 验证 100% 失败」已被证伪**：三人独立核对三处引用，全部对不上（**原始分析过程保留在 §3.2，不作为当前结论**）
- 验证：`src/**/*.js` 六节点引用 **0 命中**、`index.html` **0 命中**、`app_ui.html` 仍为 1、`npm run lint` 通过、`npm test` **39/39**、`vite build` 通过
- ⚠️ **功能缺口本身仍在，但性质是「前端没渲染」而非「后端没实现」** —— 后端 `start_validation` 是完整实现（`recording_cmd.rs:140` → `recording_service.rs:465`，73 行、3 处 `compare_exchange`、5s 超时 IPC，`grep 'Ok(0)'` 零命中），**缺的是把它渲染到报告面板的那段前端代码**；本次删除的 6 个 DOM 正是那个面板的容器（**删除 ≠ 接上渲染**）。本节不宣称「问题已解决」。
- ℹ️ 旧表述「`recording_cmd.rs:26-45` 的 `start_validation` 是 `Ok(0)` 存根」**有误**（该区间实为 `start_recording_impl` 的文档注释区），已全文订正，证据见 §3.2。

### 1.2 E2E 通道死因已修复（**修复已上库，尚未真实验证**）

三个**互相独立**的死因，均已修复，**每条都有实测证据**：

| # | 死因 | 修复 | 实测依据 |
|---|---|---|---|
| ① | 下载源 `msedgedriver.azureedge.net` **已被微软停用** | 换新域 `msedgedriver.microsoft.com`，且**必须给完整四段版本号**（只给 major 一律 404） | 亲手 `curl` 验证：旧域 **000** / 只给 major **404** / 完整四段 **200** |
| ② | `ahk_executor/AutoHotkey64.exe` 被 `.gitignore` 的 `*.exe` 排除，全新 clone 里不存在 → `build.rs` 必红 | 补 AHK 前置 | `git check-ignore` 验证过 `*.exe` |
| ③ | `tauri-driver` **无官方预编译产物**（已核实） | 加缓存减轻源码编译 | 已核实无预编译产物 |

- ✅ **修复已上库**：commit `10b57d7`
- ⏳ **是否已跑通：待定** —— 已于 2026-09-18 手动触发 **run `35309066359`**（`gh workflow run ci.yml --ref main -f run_e2e=true`），E2E job 当时 `in_progress`，**结果待出**
- ⚠️ **定位仍为手动 / 定期 / 观测**，不转阻塞门禁（历史零稳定性数据）

> 🚨 **撤回声明**：本文件此前记的「53 用例 **45 通过 / 8 失败**」「8 条全部 `AHK_TIMEOUT`」「AHK 冷启动吃掉 25s」「历史 96 次运行 0 次真跑过 → 现在第一次真正执行」等**执行结果数字，均无来源、已全部撤除**。
> 核实结果：最近 8 次 run（`35307590498` / `35305109208` / `35303599926` / `35301183381` / `35300104030` / `35299952264` / `35225025769`）的 **E2E job 全部为 `skipped`** —— **它历史上一次都没有真正执行过**。
> 在 run `35309066359` 给出真实结论前，**本报告不写任何 E2E 执行结果数字。**

### 1.3 守护有效性清单已建立

- 新建 `docs/guard-effectiveness-checklist.md`，G1–G4 逐条按四种失效形状分类：**A 预防性排除 / B 从未接入 / C 跑过期产物 / D 通道腐烂**
- **核心洞察：代价排序是「假阳性 > 静默失效 > 通道腐烂」** —— 死守护只是不报警，**假守护会主动产出假信号并掩盖真 BUG**
- 最该优先查的三条：**G3f 覆盖率棘轮**（无范围锚点，可假绿）、**G4 文档同步**（纯 `Write-Host`，结构上不可能失败）、**G3h watchdog**（观测期无截止日）
- 已登记 **TD-058**（DPI **11.3** / 档 **P0**）

### 1.4 台账 DPI 全量重算 + C9 自动校验

- 用台账自己第 12 行声明的公式 `DPI = (I + P + V + S) / ( √C × R )` 反算 **58 行，12 行对不上**
- 最刺眼：**TD-056 声明 3.0、公式算得 10.0**，整整差一个档位
- 已全量重算并重落档，**9 行档位变动**（TD-018/034/036/037/040/056/057 → **P0**；TD-033/038 → **P1**），**均为已完成状态，不影响排期**
- 新增 **C9** 校验（**硬失败、不做棘轮**）：DPI 必须等于公式值、档位必须等于阈值落档、行结构完整
- 阳性对照：注入 4 处错误**全部被精确报出**，还原后回绿
- **失效形状归类（§六 ABCD 口径）**：**B（此前无任何校验）→ 2026-09-18 新增 C9 转健康**。⚠️ **不是 C 型、也不是「C 型复活」** —— C 型是「守护在跑，但跑的是过期产物或错的判据」，而 C9 之前**根本不存在任何校验**（台账评分从未被任何东西校验过），不存在「跑」，更没有产出过假信号

> ✅ **编号冲突已裁定（team-lead，2026-09-18）**：**C9 = 台账 DPI / 档位校验**（已落地）；**G4 三副本一致性那条落脚本时改用 `C10`**（已批准）。
> ⚠️ 另：本报告此前用「TD-060」指代 G4 清单四副本问题，**该债项 ID 并不存在**（台账未登记），已改为无 ID 描述。

---

## 二、剩余问题全清单与优先级

| 债项 | 优先级 | 定性 | 真实状态 | 建议 |
|---|---|---|---|---|
| **台账自洽** | — | 🔴 度量失真 | 统计行 49 vs 实际 57；4 处状态列自相矛盾；TD-007 前提错误 | **先修**，否则后续决策全基于错误信息 |
| **E2E 通道** | — | ⏳ **死因已修复、执行结果待验证** | 三个死因均已修复并上库（commit `10b57d7`），各有 `curl` / `git check-ignore` 实测依据；**是否已跑通未验证** —— 已手动触发 run **`35309066359`**，结果待出。🚨 此前「45 通过 / 8 失败」数字已撤除 | 定位仍为手动 / 定期 / 观测，不转阻塞门禁；等 run `35309066359` 的真实结论再评估。详见 §1.2 |
| **TD-044** | P2 | ✅ **已闭环**（原建议挂起） | 已删 `index.html` 6 个死节点 + 摘 `main.js:1097` 活引用；AHK 侧 `app_ui.html` 6 个同名节点**是活代码、未动**；原 EBW-4 判据**已被证伪** | ⚠️ **功能缺口仍在，但性质是「前端没渲染」而非「后端没实现」**：后端 `start_validation` 是完整实现（`recording_cmd.rs:140` → `recording_service.rs:465`，73 行、3 处 `compare_exchange`、5s 超时 IPC，`grep 'Ok(0)'` 零命中）；缺的是把它渲染到报告面板的前端代码 —— 本次删除的 6 个 DOM 正是那个面板的容器。详见 §1.1 |
| **TD-055** | P1 | 🟠 **半闭环** | **`.sh` 已修、`.ps1` 未修**：`check-gates.sh:189-190` 已切完整模式 ✅；`check-gates.ps1:168-169` **仍是 `--no-cargo` 快速档** ❌；`ci.yml:216-217` ✅ | **补修 `.ps1`**。⚠️ Windows 是唯一能真正开发的平台（AHK 只能 Windows 跑），收益恰恰在**唯一能开发的平台上拿不到** |
| **TD-054** | P2 | 🟡 非盲区 | 63.67% 是**已测代码的渐近线**，补测闭包无收益 | 接受现状 + 文档写清口径 |
| **TD-007** | P2 | 🟡 前提错误 | `gates` job 本就是 windows，`src-tauri` 一直在跑 | 改台账表述，把采集挪到既有 windows job |
| **TD-037** | P1 | 🟢 **早已接入，欠的只是台账更新** | 根因 2026-09-16 已修；脚本**已在 G3g 清单**（`run-standalone-ahk-tests.sh:50`，`SCRIPTS` 数组第 7 项），**本地门禁 + CI 双跑** —— 本次真实 run 35299952264 步骤 18 `G3g` = **success**，实测 **38/38 通过** | 只需把台账从「待排期、7 条失败」改成「已闭环」。⚠️ **我先前写的「差一行 `#Include`」是错的，已订正**；另「必须配超时」也已订正 —— 脚本末尾有显式 `ExitApp`，不需要额外配 |
| **TD-027** | P2 | 🟡 **台账目标类型写错** | 严格统计 `Result<_, String>` **共 44 处**：28 处是 `IpcTransport` **契约**（误配成 `String` 会静默丢 `IpcError` 判别信息）、6 处进程管理、**跟配置加载无关**；真正属配置 I/O 的只有 `config_repository.rs` **9 处** | 关掉 TD-027，另立「IpcTransport → IpcError」债 |
| **TD-005** | P2 | 🟡 别先装 jsdom | `main.js` 1318 行 / 95 函数 / 151 次 `getElementById`，且**无任何 export**（整模块加载即执行，无法精准备桩）；jsdom 路线 6-8h 起步且桩库难维护 | **先抽不碰 DOM 的纯逻辑**（`valHistory` 去重封顶、`kpd` 格式化、`_updateModeState` 状态机）到 `src/lib/*.js` 并 export，用现有 `node --test` 覆盖（2-3h 就有网） |
| **TD-051** | P1 | 🟠 未来引信 | 引入 TS 后 `import type` 会被判成环（硬失败项） | **现在修，1 小时** |
| **TD-020** | P2 | 🟢 接受 | 12 high 上游无补丁，生产依赖 0 | 接受 |
| **TD-045/053** | P2/P1 | 🟢 已完成 | 台账状态滞后 | 更新状态词 |
| **G4 清单四副本**（⚠️ 台账**未登记**债项 ID，原误记为 TD-060） | P0 | 🟡 **4 份副本** | G4 清单散在 `check-gates.sh:209-217` / `check-gates.ps1:183-191` / `ci.yml:234-243` + docs 侧 2 处；`ci.yml` 那份**抬头不同且缺提示行**。6 条正文目前**逐字一致 → 正是加锁窗口** | **四步**：① 三副本互比 → ② 抽 `docs/g4-manual-checklist.md` 单一来源 → ③ 守卫升级为「各自对文档」→ ④ 再做 G4 自动化。⚠️ 第③步不能省：v1 只能证明「三份一样」，证明不了「和单一来源一样」。硬约束：**判据必须从三个文件正则提取后互比，禁止在脚本里存「标准清单」**（否则变成第五份假权威副本）。⚠️ **编号待定**：原拟用 C9，但本轮落地的 C9 已是「台账 DPI 校验」（§1.4），落脚本前请改用 **C10** |

---

## 三、🟠 两个真问题（审查当时定性，当前状态见 §一）

> ⚠️ **本节两条均已被 §一 的执行结果更新**：E2E 通道已修复并首次真跑（§1.2）、TD-044 已闭环且 EBW-4 判据已被证伪（§1.1）。
> 以下保留**审查当时的定性过程**供追溯，**不要再用于描述当前状态**。

### 3.1 E2E：「套件活、通道死」—— 措辞已由团队三方对齐定稿（**已被 §1.2 取代**）

> ✅ **经 Cody / Rex / Tessa 交叉复核定稿**：**EDA 套件本身是可执行的活代码**（本地可完整运行，最近一次全绿实测 2026-09-17：9 suite / 53 用例），**失效的是 CI 通道** —— 不是测试代码，而是 CI job 的外生依赖。

| 事实 | 依据 |
|---|---|
| 套件活 | 本地可跑，最近全绿实测 **2026-09-17**（9 suite / 53 用例）。⚠️ **本机验证，非 CI 验证** |
| ~~通道死~~（**修复前状态**） | 历史 0 次自动执行（双重手动门控 `ci.yml:529-530`）；`ci.yml:555` 下载源 `msedgedriver.azureedge.net` 已下线；`ci.yml:546` 源码编译 `tauri-driver` → **已于 §1.2 修复并首次真跑** |
| 有 flaky 前科 | `docs/known-issues.md` **BUG-7**，曾连续 9 次红 |

**定稿措辞（原文保留，作为决策轨迹）**：
> E2E 套件本地可执行（最近全绿实测 2026-09-17：9 suite / 53 用例），但 CI 通道因 `ci.yml:555` 外链 `msedgedriver.azureedge.net`（已下线）+ `ci.yml:546` 源码编译 tauri-driver，历史 0 次自动执行。**修复 ROI 高于删 job**（套件已维护，外生依赖可修），但**修好也不做 per-commit 阻断门禁**（历史 flaky 前科 `docs/known-issues.md` BUG-7），定位为手动 / 定期 / 观测。

**（2026-09-18 已落地：三个死因已修复并上库（commit `10b57d7`），但**尚未真实验证** —— 已手动触发 run `35309066359`，结果待出。🚨 本报告此前记的「45 通过 / 8 失败 / 8 条 `AHK_TIMEOUT`」**已撤除（无来源）**。详见 §1.2。以上定稿措辞描述的是**修复前**状态。）**

⚠️ **禁用表述**：「E2E 已腐烂」「E2E 是死门」「E2E 套件不可用」—— 都是错的，锅在通道不在套件。

**结论**：它**不能**作为 TD-054 的兜底，也**不能**算进本次推送的信心 —— 但这不是因为套件不行，是因为通道没开过火。

**（2026-09-18：通道死因已修复但**尚未开火** —— 最近 8 次 run 的 E2E job 全部 `skipped`；已手动触发 run `35309066359`，结果待出。在拿到真实结论前，它**不计入**本次推送的信心。详见 §1.2。）**

### 3.2 TD-044：是「没接线的功能」，不是死代码（**EBW-4 判据已被证伪，见 §1.1**）

> ⚠️ **本节的 EBW-4 判定已于 2026-09-18 下午被证伪**：三人独立核对下列三处引用（`main.js:1075-1087`、`main.js:1068`、`app_ui.html:3477`），**全部对不上**。
> 保留原始论证过程供追溯；**当前结论以 §1.1 为准** —— TD-044 已按「删除 6 个死节点 + 摘活引用」闭环，AHK 侧同名节点是活代码、未动。

原始定性（**原文保留，作为决策轨迹**；当前结论见 §1.1）：真实情况比"DOM 悬空"严重 ——

| 证据 | 位置 | 含义 | 事后核对 |
|---|---|---|---|
| `startValidation` 只接受字符串 | `main.js:1075-1087` | 而 `main.js:1068` 传的是**数字** | ❌ 引用对不上 |
| Tauri 版**无** `startValidation` 调用 | `main.js` | 与 AHK 版不同 —— 它甚至不接受数字 | ❌ 引用对不上 |
| AHK 版同样接受数字 | `app_ui.html:3477` | **同一缺陷跨两套 UI 存在** | ❌ 引用对不上 |
| `renderValHistory` 用 `&` 拼布尔 | `app_ui.html:3526-3528` | 应为 `&&` | 未纳入本轮处置 |
| `valReport` 无条件 `display="none"` | `main.js:1097` | 面板恒隐藏 | ✅ 属实，已摘除 |

**~~判定为 EBW-4：Tauri 版按键验证 100% 失败率。~~** 该判据已作废。

⚠️ **仍然成立的部分**：**验证跑完后仍然没有报告面板**（删除 ≠ 接上渲染）。
✅ **已订正（2026-09-18，team-lead 逐条核实 + Docu 独立复核）**：

> **后端实现是完整的**：`start_validation_impl` 在 `recording_cmd.rs:140`，校验 `group_id.trim().is_empty()` 后委托给 `recording_service::start_validation`（`asd-application/src/recording_service.rs:465`）—— 该函数 **73 行**，含 **3 处 `compare_exchange`**（并发保护与失败回滚）和带 **5 秒超时** 的 `send_ipc_and_wait`，全文 `grep 'Ok(0)'` **零命中**。
> **真正的缺口只在前端**：验证跑完后**没有任何代码渲染报告面板**，所以用户看不到结果。
> ⚠️ **旧表述有误**：「`recording_cmd.rs:26-45` 的 `start_validation` 是 `Ok(0)` 存根」—— `recording_cmd.rs:26-45` 实际是 `start_recording_impl` 的文档注释区（函数体在 `:31`），与 validation 无关。

| 断言 | 核实结果 |
|---|---|
| `:26-45` 不是 validation | 是 `start_recording_impl` 的 `# Errors` 文档注释（函数体 `:31`） |
| `start_validation_impl` 在 `:140` | ✅ `recording_cmd.rs:140` |
| 实现在 `recording_service.rs:465`，73 行 | ✅ `pub fn start_validation` 在 465；函数体 73 行 |
| 3 处 `compare_exchange` | ✅ 486 / 508 / 525 |
| 5 秒超时 | ✅ `send_ipc_and_wait(&cmd, Duration::from_secs(5))`（`:498`） |
| 不是存根 | ✅ `grep 'Ok(0)'` 在两个文件均**零命中** |

**（2026-09-18 已落地：TD-044 已按「删 6 个死节点 + 摘 `main.js:1097` 活引用」闭环；AHK 侧 `app_ui.html` 同名 6 节点是活代码、未动；原 EBW-4 判据已被证伪。详见 §1.1。以上原始定性描述的是**审查当时**的判断，不作为当前结论。）**

---

## 四、🟡 被纠正的三个认知

1. **TD-054 的 63.67% 不是盲区，是渐近线**（Archi + Tessa 一致）。`src-tauri` 命令层是一行委派，语义在 `asd-application`（已在 89.05% 棘轮内）；2124 个未执行函数绝大多数是 `generate_handler!` 生成的闭包，补测它们测的是 serde + 转发，**缺陷检出率≈0**。真正的剩余风险只在 `ipc.rs` + `watchdog.rs`（约 3100 行，Windows 命名管道与进程重启路径）。
2. **TD-007 不需要申请 Windows runner**（Rex 核实）。`gates` job 本就在 `windows-latest`，`src-tauri` 单测一直在跑。台账那句话是错的，应改为「把 coverage 采集迁到既有 windows job」。
3. **TD-037 早已接入 G3g，台账滞后才是唯一欠账**（Tessa 更正 + 我用真实 CI 复核）。`run_all_tests.ahk`（G3b，722 用例）确实没收它，但**它走的是另一道门 G3g**（`run-standalone-ahk-tests.sh` 的 `SCRIPTS` 数组第 7 项）—— 本地门禁与 CI **都在跑**，本次真实 run 35299952264 的步骤 18 `G3g` 实测 **success**、38/38 通过。⚠️ 我先前判断「差一行 `#Include`」是**错的**：G3b 与 G3g 是两道**并列**的门，不能用 G3b 收不收录来判断一个脚本有没有被门禁覆盖。

---

## 五、发布侧：推送 Go / No-Go

**判定：Conditional Go** —— 一次性推 195 个提交，**不 squash、不分批**。

> ℹ️ 本节为**推送前的判定依据**（一次性推、不 squash、不分批），推送执行情况以最新状态为准 —— 报告此前记的「推送失败 / 196 提交待推」**已过期，已移除**。

- **不 squash 的理由**：细粒度 `fix(debt)`/`perf` 提交是定位回归的唯一粒度，压平后任何回滚都是全量。
- **不分批的理由**：切分点无法自证干净（本仓库有 CI 连续 9 次红的记录），且每次推送都可能撞代理 502。
- **两个前提**：① G4 人工确认完成；② 确认 `.workbuddy-ai/memory/` 公开可接受（含本地绝对路径与内部决策；已扫描无真实凭据，作者邮箱为 `dev@autohotkeydemo.local`）→ ✅ **已处置（本轮下午）**：决定**不公开**，已用 `git rm -r --cached` 退出版本控制 + 加入 `.gitignore`，commit `20c5537`，**工作区 14 个文件完好**。

推送后必看的 4 个 job：四闸门(windows) / AHK Benchmark / 依赖安全审计 / 前端 lint。
⚠️ **E2E 仍然恒 `skipped`** —— 最近 8 次 run 全部 skipped；死因修复已上库（`10b57d7`）并已手动触发 run `35309066359`，**结果待出**（§1.2）。它**不是阻塞门禁**，失败不阻断合并。

```bash
bash scripts/push-and-verify.sh main   # 仓库已有，含重试 + 远端 sha 核验 + 等 CI 结论
```

---

## 六、排期建议：先修通道，再接 watchdog

> ⚠️ 「先修通道」已于本轮执行完（§1.2）。本节保留原排期论证供追溯。

**证据强度分档**（Archi 更正：我原先对两者打同样的「陈旧折扣」是错的）：

| 项 | 绿灯证据日期 | 距当前 HEAD | 强度 |
|---|---|---|---|
| E2E 套件 53 用例全绿 | **2026-09-17** | 1 天 | **硬** |
| watchdog 15 条 `--ignored` 全绿 | ~~2026-09-13~~ → **本轮已重验：15 passed / 0 failed / 13.39s，CI G3h 步骤 success** | ~~195 个提交~~ → **已重验** | ~~弱（须重验）~~ → **硬（已重验）** |

→ **若只能先做一个：先修 E2E 通道**（改下载源 + 预编译/缓存 `tauri-driver`，外生依赖、工作量小、证据新鲜）。watchdog 接入按「先推 195 → CI 绿 → 单独 PR」推进，首次跑红按归因三分处理。

**（2026-09-18 已落地：E2E 通道三个死因已修复并上库（`10b57d7`），**执行结果待验证**，已手动触发 run `35309066359`（§1.2）；watchdog 15 条已接入 G3h，本地 15 passed / 13.39s、CI success。本节的「先修通道」排序已执行到「修复上库」，**尚未拿到执行结果**。）**

**「预防性排除清单」应升格为「守护有效性清单」** —— 这族问题的共同点不是"排除"，而是**「守护与它声称守护的对象失联，且没有任何信号暴露这种失联」**：

| 形状 | 实例 | 代价 |
|---|---|---|
| A 预防性排除 | e2e 整目录、watchdog `#[ignore]` | 静默失效 |
| B 从未接入 | AHK `tests/` 下 7 个脚本；**台账 DPI / 档位**（此前无任何校验）→ **2026-09-18 新增 C9，转健康** ✅ | 静默失效 → 已消除 |
| C **跑过期产物** | TD-046：exe 停在 06-29 → **12 条假 HIGH** | **假阳性（最贵）** |
| D 通道腐烂 | E2E CI 通道 → **2026-09-18 已修复并首次真跑** ✅ | 静默失效 → 已消除 |

> ⚠️ **两条易误归类，特此钉死**：
> 1. **台账 DPI / C9 不是「C 型复活」** —— C 型是「守护在跑但跑的是过期产物或错判据」；C9 之前**根本不存在任何校验**，压根没有守护，也从未产出过假信号，属 **B 的变体（无守护）→ 转健康**。
> 2. **D 通道腐烂（E2E CI 通道）仍处于「已修复但未验证」**：死因已排除并上库（`10b57d7`），但最近 8 次 run 仍全部 `skipped`，已手动触发 run `35309066359` 待结果（§1.2）。🚨 **在拿到真实执行结论前，不得描写任何 E2E 执行结果**（本报告此前记的「45 通过 / 8 失败」已撤除）。

**代价排序：假阳性 > 静默失效 > 通道腐烂**。死守护只是不报警，**假守护会主动产出假信号、消耗人力、并掩盖真 BUG**（TD-047 就是在揭穿 TD-046 那层假绿之后才浮出来的生产 BUG）。叫「排除清单」会漏掉 C。

登记强制项应有**两条腿**：① `expires`（管时间维度，本仓 `check-graph-baseline.py:113-121/384-386` 已有先例）；② **阳性对照**（管有效性维度，防"到期前一直在假装有效"）。

💡 **「让已写但从未真正执行的测试第一次真正执行」在本仓的战绩：四次全中奖、零落空** —— AHK 7 个脚本 → 3 个 BUG（TD-034/035/036）→ `test_joystick` 接入修掉一批陈旧断言 → TD-046 揭穿假绿 → TD-047 生产 BUG。这是本次「行动 A」最有力的论证。

**测试策略应从「按文件补」升级为「按品类规避」**（Tessa）：

| 品类 | 状态 | 缺口 |
|---|---|---|
| ① 参数 / 契约 | 已覆盖 | — |
| ② 状态时序（mtime / force 回写、config 变更重发） | 5 个 mock 手工断言，**未覆盖真实 `ConfigRepository`** | 应升格为**行为契约测试**：同一组契约断言在 mock 与真实实现上**各跑一遍**（表格驱动），证明 mock 与真实一致 |
| ③ IPC 跨进程契约 | ✅ **本轮 C8 已解决**（2026-09-18） | — |
| ④ 端子事件（disconnect / rpc-error / remote-close） | **0%，无守护** | 最危险：真实场景会**静默失联** |

→ **品类 ④ 是下一轮测试投入的首选** —— 它与「守护有效性」同源：失联且无信号。品类 ② 的修法不是补更多断言，而是**把 mock 的行为契约化**。

---

## ✅ 行动清单

| # | 行动 | 负责角色 | 紧急度 | 备注 |
|---|------|---------|--------|------|
| 1 | 修台账自洽：统计行 49→57、4 处状态词、TD-007 前提纠错、快照 719→722、数字改指针 | Docu | P0 | 🟡 **部分落地**：DPI 全量重算 + C9 校验已落地（§1.4）；**统计行 / 状态词 / 快照数字 / 数字改指针仍待办**。否则后续决策仍基于错误信息 |
| 2 | TD-044 拆两件：① 删 6 个死 DOM **并同步改 `main.js:1097`**（1h）；② EBW-4（`startValidation` 类型不匹配）**独立登记** | Cody | P0 | ✅ **已落地**：① 已删 6 个死节点 + 摘 `main.js:1097`（§1.1）；② **EBW-4 判据已被证伪，不再单列**。⚠️ **功能缺口仍在**，但性质是**前端没渲染**（后端 `start_validation` 已是完整实现），见 §1.1 |
| 3 | 定 E2E 死门禁：修 / 删 / 留 | Rex | P1 | 🟡 **修复已上库（`10b57d7`），执行结果待验证** —— 最近 8 次 run 仍全部 `skipped`；已手动触发 run `35309066359`，**等真实结论再定是否转门禁**。🚨 此前「45 通过 / 8 失败」数字已撤除（§1.2） |
| 4 | 修 TD-051 `import type` 过滤 + 阳性对照（1 小时） | Archi | P1 | 顺带修现存假阴性（`export {x} from` 漏检） |
| 5 | G4 清单固化成 `docs/g4-manual-checklist.md` | Docu | P2 | 待办。脚本只打印文档路径+编号，避免多处漂移。✅ 守卫编号已裁定为 **C10**（C9 归台账 DPI 校验）。⚠️ 该问题在台账中**尚无债项 ID**，登记时另编 |
| 6 | 等 run `35309066359` 的 E2E 真实结论，再定通道是否转门禁 | Rex | P1 | ⏳ **结果待出**；在此之前不写任何 E2E 执行结果数字（§1.2） |

---

## ⚠️ 待完善 / 已知局限

- Rex 拿不到 CI 原始日志（job logs 接口 `403 Must have admin rights`），"src-tauri 单测在 CI 上跑了"是基于 workflow 配置 + workspace members 的推断，终局证据需推送后用 `gh run view <id> --log` 坐实。
- ⚠️ **TD-037 的断言总数三方不一致**：台账记 32 条、Tessa 实测 38/38、Cody 复刻 34/34。差异未归因（可能是测试框架版本或计数口径不同），**接入门禁时以实跑输出为准**，不要沿用任何一个历史数字。
- TD-044 已按「删 DOM」执行（6 个死节点 + `main.js:1097` 已同步摘除，§1.1）。**未走「接回渲染」路径** —— 若将来要接回，**后端已具备完整实现**（`recording_cmd.rs:140` → `recording_service.rs:465`），只需补「把结果渲染到报告面板」的那段前端代码。⚠️ **功能缺口本身仍在**：验证跑完后仍无报告面板。
- Cody 本次改动（TD-037 生产缺陷修复）**尚未提交**，且他发现 AHK 完整套件实跑 **723**（比 test-map 登记的 722 多 1，是新接入的 1 条确认断言）——**test-map 三处需同步**。
- Docu 建议的「状态单元格 ≥2 个互斥状态词即 FAIL」门禁尚未实现。
- ⚠️ **已订正**：先前记的「TD-027 有 2080 处调用点」**既非 Cody 产出、数字也不成立** —— 严格统计 `Result<_, String>` 共 **44 处**（28 IpcTransport + 6 进程管理 + 9 配置 I/O + 1 其他），已逐条核实。
- ✅ **编号已裁定**：C9 = 台账 DPI / 档位校验（已落地）；**G4 三副本一致性守卫落脚本时改用 `C10`**（team-lead 已批准）。
- ⚠️ **已订正**：G4 清单四副本问题**在台账中并无债项 ID**（原误记为 TD-060），登记时需另行编号。
- ✅ **已订正**：「后端 `recording_cmd.rs:26-45` 的 `start_validation` 只返回 `Ok(0)`、是存根」的说法**有误** —— `recording_cmd.rs:26-45` 实为 `start_recording_impl` 的文档注释区（函数体 `:31`）；真正的 `start_validation_impl` 在 `:140`，委托给 `recording_service.rs:465`（73 行、3 处 `compare_exchange`、5s 超时 IPC），**后端实现完整**，缺口只在前端渲染。已全文替换（详见 §3.2 证据表）。
- ⚠️ **E2E 执行结果仍未知**：最近 8 次 run 的 E2E job 全部 `skipped`；修复已上库（`10b57d7`）并手动触发 run `35309066359`，**结果待出**。此前「45 通过 / 8 失败」等数字**已撤除**。

---

## 📚 数据来源 & 成员产出索引

- **Cody**：TD-044 建议 **B：删 DOM 但必须同步改 `main.js:1097`**（并补出台账漏记的 `intervalCanvas`、指出无图表库）+ TD-027 **实测 44 处**（28 IpcTransport + 6 进程管理 + 9 配置 I/O + 1 其他）→ 关掉 TD-027、另立 IpcTransport 债 + TD-005 建议**先抽纯函数（2-3h）**再谈 jsdom（6-8h）
  - ✅ 其 TD-044 建议 B 已于本轮落地执行（§1.1）。
  - ⚠️ **归属更正（2026-09-18 澄清，已订正）**：以下**均非其产出**，已从本条移除 —— 「TD-037 38/38」「测试桩问题」「TD-027 2080 处」「TD-005 延后到 Tauri 2.0」「EBW-5（`cfg1` / `CFG2` case 混淆）」「EBW-4 生产缺陷」。
  - ℹ️ TD-027 的**正确数字是 44 处**（其中属配置 I/O 的只有 9 处），非 2080 处；TD-005 他的建议是「**先抽纯函数，2-3h**」，非「延后到 Tauri 2.0」。
- **Archi**：TD-054 选 ③ 为主 + ① 缩小到 ipc/watchdog；TD-007 只需改 coverage 的 runs-on；TD-051 现在修（1 小时）；整体判断「最该防的是新增，不是继续补债」
- **Rex**：推送 Conditional Go + 检查清单 + 回滚方案；核实 `gates` job 是 windows-latest；核实 E2E 为死门禁（下载源已停用）
- **Tessa**：TD-037 实测 38/38 通过；TD-054 三选一评估；测试侧 1 天投向建议
- **Docu**：台账 10 项文档债清单；统计行 49 vs 57；G4 清单结构建议；确认无「89.05% = 全仓覆盖率」误导表述；**本轮报告更新（§一「本轮已落地」+ 归属订正）**
- **本轮落地（2026-09-18 下午，详见 §一）**：TD-044 清理与验证（§1.1）／E2E 通道**三个死因修复并上库**（commit `10b57d7`，⏳ **执行结果待验证**，已手动触发 run `35309066359`，§1.2）／`docs/guard-effectiveness-checklist.md` 守护有效性清单（TD-058，DPI **11.3** / P0，§1.3）／台账 DPI 全量重算 + C9 校验（§1.4，归类 **B → 健康**）
- 🚨 **本报告撤回声明**：此前曾记「E2E 首次真实执行 45 通过 / 8 失败、8 条 `AHK_TIMEOUT`」—— **该组数字无来源，已全部撤除**；经核实最近 8 次 run 的 E2E job 全部 `skipped`，**它历史上一次都没有真正执行过**。真实结论以 run `35309066359` 为准。
- **主理人独立复核**：`git status`=0 改动；报告文件 `passed:38/failed:0`；`webview2_manager.ahk:18` #Include 已存在；TD-044 旧实现在 `app_ui.html:3402/3473` 仍存活
