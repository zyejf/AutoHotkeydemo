# ASD 技能管理器 v4.0 生产就绪度评估报告

**日期**：2026-09-19
**工作流**：工作流 1（代码审查）+ 工作流 4（部署前 Go/No-Go）+ 工作流 5（技术债）组合
**参与成员**：Cody（代码审查师）、Archi（架构师）、Rex（SRE）、Tessa（测试专家）、Docu（技术文档师）
**编排**：甄宇航（Zhen）· 工程督导

---

## 📌 TL;DR（执行摘要）

- **总体判定：合入 `main` 可 Go；对外公开分发安装包 No-Go。** 两个结论不冲突 —— 代码与单测质量处在同类项目上游水平，但**发布链路、合规、可运维、用户侧文档四个维度各自独立地不达标**。
- **严重度分布**：🔴 阻断 **12** 项（去重后）／🟠 高 **6** 项／🟡 中 **10** 项／🟢 低 **5** 项。
  （「无 updater」经 Archi 自我更正后**由阻断降为中** —— 研究稿把 updater 标为 P2「按需评估」，且同行并列的另外 3 个插件都落地了，说明那是评估清单而非承诺清单。真正阻断的是**没有 GitHub Release 步骤**：无 updater 不是问题，「手动重新下载」这条路不通才是。）
- **最硬的证据不是任何一条缺陷，而是"零执行痕迹"**：`target/release/` 下无 exe、无 bundle、无 NSIS 包（本机实测）；release job 从未执行；CI 上 E2E 53 个用例零执行。**这条链路从头到尾没有一丝真实运行过的痕迹。**
- **放行成本远低于预期**：约 **3～5 人日**，且大部分不改代码逻辑。
- **一句话概括：内环很结实，外环没有门**（Docu 提出，主理人认为它适用于整个项目而不只是文档）—— 工程侧文档 1956 行 + `AGENTS.md` 1463 行 + ADR 规范严谨 + C3/C3b/C13 自动守一致性；代码侧 465 条测试全绿、Tauri command 路径零 panic、watchdog 重启有上限与退避。**但所有这一切都是给"已经上手的人"准备的**：陌生人装不上、用不了、出问题看不见，维护者发不出去、退不回来。
- **本次评估推翻了我自己派单时的 3 个锚点**，并定位到 1 处把修复成本夸大了一个数量级的错误注释。

---

## 🎯 核心结论卡片

| 项目 | 内容 |
|------|------|
| **整体评级** | 🟡 **有条件通过** —— 合入 Go / 对外分发 No-Go |
| **分域判定** | 🟢 代码质量 🟢 单元与集成测试 🟡 架构｜🔴 合规与分发 🔴 可运维性 🔴 文档与用户侧 🔴 端到端与发布验证 |
| **阻塞项数量** | **12**（去重后；「无 updater」经 Archi 自我更正后降级为中危，见 §2.2） |
| **今天发布的把握度** | **C+**（Tessa 评级） |
| **关键行动项** | 13 条，分 4 阶段，约 3～5 人日 |
| **建议下一步** | 先做「阶段 0 合规最小集」（约 0.5 人日，纯加文件不改代码），再决定是否继续投入发布链路 |

---

## 一、评估范围与前提假设

### 1.1 「生产级」在本报告中的定义

本项目是**面向个人用户的 Windows 桌面工具**（按键连招管理），**没有服务端、没有集群、没有值班团队**。因此本报告**没有套用 SaaS 生产标准**（不要求 Prometheus/Grafana/多可用区/99.9% SLO）。判定口径为：

> 一个从未见过本项目的**真实用户**，能否**装上 → 用起来 → 出问题时自救或被看见**；以及**维护者**能否**构建出包 → 发出去 → 出问题能回滚**。

### 1.2 评估边界

| 维度 | 负责人 | 主要手段 |
|---|---|---|
| 代码安全与健壮性 | Cody | 源码逐行 + 465 条测试实跑 |
| 架构与分发合规 | Archi | 配置取证 + md5 比对 + 许可证分析 |
| 可运维性 | Rex | 关键路径 grep + 故障恢复推演 + 预案批判性审阅 |
| 测试与质量门禁 | Tessa | **四道闸门逐道阳性对照** + 覆盖率重取 |
| 文档与发布物料 | Docu | 文档走查 + GitHub 首页 WebFetch + 命令实证 |

### 1.3 前提与局限

- 全部为**本机实测**（Windows + Git Bash）。凡写"实测"均未上 CI —— CI 侧因 `gh` 未登录无法核实分支保护。
- 未能实跑的部分已**逐条标注「推断」**，并在行动清单里列为"待实测确认"（例如看门狗重启后热键是否真的不恢复）。
- 覆盖率数字为重取值，与文档登记值存在过期差（见 §5.3）。

---

## 二、🔴 阻断项清单（去重后 12 项，按域归类）

> 去重说明：Rex 的「release 从未执行」与 Archi 的「无 GitHub Release 步骤」合并为一项；Cody 的「看门狗重启失败无前端事件」与 Rex 的「Failed 终态 UI 零提示」为同一根因，合并为一项。

### 2.1 合规与分发域（3 项）

| # | 问题 | 证据 | 判定 | 为什么是阻断 |
|---|---|---|---|---|
| **B1** | **公开仓库无 LICENSE** | 仓库根 `ls LICENSE*` 无输出；GitHub 首页实测无 License 徽章 | 实测 | 默认「保留所有权利」，他人无权合法使用/贡献/二次分发，与「已转公开仓库」自相矛盾 |
| **B2** | **NSIS 包打进 GPLv2 二进制，但包内无许可证文本与源码说明** | `tauri.conf.json:42` `bundle.resources` 含 `ahk_executor/AutoHotkey64.exe`；打包 exe 与 vendored 二进制 **md5 一致**（`52086b80…`）；包内无 license 键 | 实测 | **Q1（是否须随附）确定答「是」**，与 Rust 侧是否被传染无关（见 §3.1） |
| **B3** | **release job 从未执行，且 ci.yml 全文 913 行无任何 GitHub Release 步骤** | `ci.yml:786-913` 只 `upload-artifact`、retention 90 天；`target/release/` 下无 exe/bundle/NSIS 包 | 实测 | 唯一"分发渠道"是一个**会过期的 CI 产物**，连"让用户重新下载"的落点都不存在；无发布即无回滚 |

### 2.2 可运维域（3 项）

| # | 问题 | 证据 | 判定 | 为什么是阻断 |
|---|---|---|---|---|
| **B4** | **执行器进入 Failed 终态后 UI 零提示，且无自助恢复** | `system_cmd.rs:135` 有 `get_executor_status`、`api.js:38` 有 `getExecutorStatus()`、`api.js:43` 有 `resetWatchdog()`，但 `grep` `main.js`/`index.html` **零命中** | 实测 | 核心功能全失而界面一切正常；用户唯一的恢复手段是杀掉重开，而没人告诉他出事了 |
| **B5** | **AHK 子进程重启后热键不重注册** | `watchdog.rs:1279-1299` `restart_child` 只 kill+spawn；`active_hotkeys` 在 `state.rs:84` 无重放路径 | **推断**（无重放路径已实测；"重启后确实失效"未经端到端实测） | watchdog「重启成功」≠「功能恢复」—— 自动重启对最终用户几乎无实际价值 |
| **B6** | **配置损坏时静默回退默认配置，且不自动尝试恢复备份** | `lib.rs:718-723` 只 `tracing::error!`，setup 期零 `emit` | 实测 | 用户视角就是"我的数据丢了"，且多半没备份可恢复 |
| ~~**B7**~~ | ~~无 updater~~ → **降级，移出阻断清单**（见 §5.2）：研究稿把 updater 标为 **P2「按需评估」**，且同行并列的另外 3 个插件（global-shortcut / dialog / fs）**都已落地**，updater 是唯一未落地项 → 那一行是**评估清单而非承诺清单**。桌面工具靠手动下载升级很常见，**无 updater 本身不是阻断** | — | — | **真正阻断的是 B3**：无 updater 时"手动重新下载"是唯一升级路径，而这条路径的落点（GitHub Release）**不存在** |

### 2.3 文档与用户侧域（4 项）

> **去重说明**：Docu 独立评定的文档域阻断为 **6** 项（1 阻断 / 4 高 / 5 中 / 2 低 = 共 17 条），其中 **#4（无 LICENSE）与 #6（GPLv2 打包无声明）已分别并入 §2.1 的 B1 与 B2**，此处不重复计数。

| # | 问题 | 证据 | 判定 | 为什么是阻断 |
|---|---|---|---|---|
| **B8** | **无 README** | 仓库根 `ls README*` 无输出；GitHub 首页实测为 22 目录 + 37 文件列表，被 `fix1_verified.png` 等调试截图占据 | 实测 | 陌生人落地后无法得知这是什么、怎么装；首屏观感像未整理的个人草稿 |
| **B9** | **全仓零用户手册** | 全仓 md 搜 `quickstart\|用户手册\|使用手册` → **0 命中**；应用内 7 个页面搜 `帮助\|FAQ` → **0 命中** | 实测 | 10 种执行模式、热键、摇杆、备份恢复均无说明；装完不知道怎么用 |
| **B10** | **唯一排障表要求用户调内部 API** | `developer-guide.md:676-685` 六行：`get_executor_status` / `active_hotkeys` / `mark_shutting_down` | 实测 | 终端用户**无执行路径**；叠加无遥测（TD-076）→ 故障与反馈双盲 |
| **B11** | **安装前置未说明，且文档与配置矛盾** | `tauri.conf.json:52-55` `webviewInstallMode = downloadBootstrapper + silent`；`developer-guide.md:700` 却写「Windows 10+ 内置」 | 实测 | 离线/内网用户装不上或双击没反应，且 `silent:true` 下失败无提示 |

### 2.4 端到端与发布验证域（2 项）

| # | 问题 | 证据 | 判定 | 为什么是阻断 |
|---|---|---|---|---|
| **B12** | **E2E 53 个用例在 CI 上零执行** | 结构核查：e2e 与 release 均结构性不可达（2026-09-16 曾核对 96 次运行记录） | 实测 | 集成风险完全没有自动化兜底 |
| **B13** | **发布产物零冒烟** | `lib.rs` 覆盖率仅 **22.84%**，启动路径无自动化验证 | 实测 | 出包后能不能启动、能不能执行一个连招，无人验证 |

---

## 三、🔍 关键专项：GPLv2 合规的两问拆解法

这是本次评估中**推理最严密、也最容易搞错**的一处。Archi 与 Docu 二轮交叉后确定了正确的定性方式：

| | 问题 | 结论 | 独立性 |
|---|---|---|---|
| **Q1** | 分发 GPLv2 二进制，是否须随附许可证文本 + 源码说明？ | **确定：是** | **与 Q2 无关**。GPLv2 §2 的 mere aggregation 免除的是"另一件作品"，**不免除**你对被分发的 GPL 程序本身的 §1/§3 义务 |
| **Q2** | 自研 Rust/AHK 代码是否被传染、须以 GPL 开源？ | **不确定，需法务意见** | 量级远大于 Q1（答"是"则整个许可证策略重做） |

### 3.1 为什么这个拆解改变了行动

Docu 初稿把 GPL 定为「高」并附条件"若法律确认触发则升阻断"。**这个条件挂错了问题** —— 它会让读者以为"也许根本没义务"，从而**推迟那个最便宜的修复**（包内附许可证文本，约 1 小时），而该修复在 **Q2 两种答案下都必须要做**。

正确定性：**B2 定阻断，备注 Q2 需法务意见，二者独立不互相阻塞。**

### 3.2 取证链（Archi 实测，非推断）

- AHK 是 **vendored 而非 submodule**，181 个文件已入库（source 168 + sln/vcxproj 编译脚本）。
- 打包 exe 与 vendored 二进制 **md5 一致**（`52086b80…`），CI 现下载官方版 → **provenance 干净**。
- §3「源码物料」**其实已经齐备**（随公开仓库分发），**只差包内一句话说明**。
- Rust/AHK 是独立进程 + IPC、`build.rs` 无链接 → **大概率构成 mere aggregation，Rust 侧不被传染**（**推断**，建议法务优先复核；若推翻则许可证策略重做）。

---

## 四、⚠️ 贯穿性发现：「东西写了，但没接上」

这是本次评估**最有价值的产出**，因为它不是 13 条孤立缺陷，而是**同一个系统性病灶的 10 处表现**。

| # | 表现 | 来源 |
|---|---|---|
| 1 | `getExecutorStatus` / `resetWatchdog` 命令与前端封装都写好了，**零调用** | Rex |
| 2 | `ipc.rs:63-69` 注释称"interprocess 不支持 DACL，需绕过库" —— **实测库原生支持**（`ListenerOptionsExt::security_descriptor`）。该错误前提使 TD-071 修复成本被**夸大为架构级**，实际**是一行代码** | Cody |
| 3 | 全仓 2 条 ADR，**零条被 Accepted** —— 不是"某条没生效"，而是**ADR 流程从未走完过一次** | Archi |
| 4 | 研究稿提过 → 落地没做 → 无留痕。**⚠️ 证据强度须分级，不能因模式好看而并列**：**MSI = 强**（研究稿 `:781` 给了具体配置值 `["nsis","msi"]`，落地只取 nsis）；**updater = 弱**（研究稿 §11.3:895 仅列为"P2 按需评估"，不是明确规划）。**Archi 已主动自我更正**，补了决定性佐证：`:895` 同一行并列 4 个插件，其中 3 个都落地了，**updater 是唯一未落地项** → 证明那一行是"按需评估"清单而非承诺清单 | Archi / Docu（分级由 Docu 提出、Archi 自我更正确认） |
| 5 | 文档一致性机制建了（C3/C3b/C13），**唯独版本号（TD-078）没守** | Docu |
| 6 | 覆盖率棘轮建了，但基线不随提交收紧，**留下约 3pp 静默下降额度** | Tessa |
| 7 | **G4 闸门结构上不可能红** —— 两端都只是打印清单，它是纸面核对表不是自动闸门 | Tessa |
| 8 | `CONTROL_HOTKEYS.emergency`（默认 F10）**三处均无注册**，只是 UI 上一个按钮 → UI 卡死时 100% 无效 | Rex |
| 9 | `ValidateGroupHotkeys` 死入口，生产零调用却内含两条真实规则（TD-079） | 前期已登记 |
| 10 | **我自己上一轮漏修** `developer-guide.md` §4.8（`1681-1684`）的日志路径 | 主理人（本机实测复核确认） |
| 11 | **门禁晋级动作已做，前置条件未留痕**：`ci.yml:159-164` 规定 watchdog 进程生命周期用例摘 `#[ignore]` 的前提是「连续跑绿 3–5 次后再转硬门禁」。实测全仓 `#[ignore]` **属性已为 0** —— 注意 `grep -rnF "#[ignore]"` 命中的 8 处**全是注释提及**、无一处是实际属性（`bridge_tests.rs:189` 那条说的是 BUG-4 已于 2026-09-13 删除的死测试）；摘除者是今日提交 `721159a`。**晋级动作已完成，但留痕只有 1 次本地 + 1 次 CI —— 且在三套口径冲突解决之前，连"是否已满足"都无法判定**（不知道该按哪一套算；这与覆盖率"不知道按哪条命令取基数"是同一个坑）。⚠️ 更麻烦的是**规定本身互相冲突，共三套**（Tessa v3 升级，原为两套）：① `ci.yml:164` = 连续跑绿 **3–5 次**；② `watchdog_integration_tests.rs:23-24` = 本机与 CI **各连续 ≥10 次**；③ 实际执行依据 = **「13 秒不慢」**。**晋级实际按的是最弱那条**，而留痕（1 次本地 + 1 次 CI）离最严判据差得最远。**三条反证**（行号经主理人逐行实证核对，此前引用的 24-25 / L25 / L15-27 **均错一位**）：**反证一（自我违反）**—— `:24` 末句自己规定「摘的同时必须把本段判据一并删除或改写，不许留下过期说明」，而该判据段（**`:14-26` 至今仍在文件里**）；**反证二**—— 该段开头 `:15-17` 明确写着「依赖真实外部进程的用例有真实失败面，摘了等于把偶发红引进 CI，是用一种债换另一种债」，而这 15 条**恰恰全是真实进程用例**；**反证三**—— 判据 5（`:25-26`）要求「能用替身覆盖就优先用替身，把真实进程用例降级为可选的手动验证」，直接摘 ignore 进 CI **与判据 5 同样拧着** | Cody 发现 / Tessa v3 升级 / 主理人逐行实证核对并修正行号 |

**判读**：单独看每一条都是小问题；合起来说明本项目的失效模式是**"产出物与执行链脱节"** —— 文档、注释、接口、ADR 都能被写出来，但**没有任何机制保证它们被接上、被执行、被复核**。

⚠️ **归纳这条模式时必须守住一条纪律：模式不能压过证据强度。** 第 4 条是最典型的例子 —— "研究稿提过、落地没做"这个模式很好认，但 MSI 是**强证据**（给了具体配置值），updater 只是**弱证据**（仅列为"P2 按需评估"）。若为了凑齐一个漂亮的模式而把弱证据写成强证据，这份报告自己就犯了它正在批评的毛病。

**这也直接决定了修复策略**：本轮的放行路径**不应该**是"再补更多文档/更多 ADR"，而应该优先**把已写好的东西接上**（见 §6 阶段 1）。Archi 的建议正中要害：**在补任何新 ADR 之前，先把那 2 条转 Accepted，否则只会得到更多"写了但不生效"的文档。**

---

## 五、🟢 分域详判（含"超出预期"的正面记录）

### 5.1 代码安全与健壮性 —— 🟢 接近生产级（0 阻断 / 2 高危）

**实测数据**：465 条测试全绿（406 Rust + 59 前端）；生产代码 panic 点**仅 1 处**（`lib.rs:829` 入口 `expect`），**Tauri command 路径零 panic**。

**2 项高危**：
1. 看门狗重启 10 次失败后只写 error 日志、无前端事件（已并入 B4）。Cody 特别指出**项目里已有 `ipc:listener-failed` 的现成模式，照抄即可**，不需要新设计。
2. IPC 管道无 DACL（TD-071），同权进程可冒充 AHK —— **修复是一行代码**（见 §4 #2）。

**我派单时被纠正的两点**（记录以免后人重踩）：
- TD-072 **不是**路径遍历风险 —— `validate_file_path` 已拦住 `../`/UNC/相对路径，4 个入口全调用。真实缺口是"无根目录白名单 + 未 `canonicalize`"，且**这 5 条安全分支零测试覆盖**。
- CSP 在 `tauri.conf.json:22`，不在 `index.html`。
- 另：首次 `cargo test` 触发 rustc 1.95.0 ICE（**编译器 bug，非代码问题**），`CARGO_INCREMENTAL=0` 后正常。

### 5.2 架构可持续性 —— 🟡 接近生产级（0 架构阻断）

**超出预期的正面记录**：日志按天轮转 + 7 份保留 + 3 条契约测试、panic hook 清理子进程、IPC 心跳、IPC token 认证、dependabot 四类、cargo/npm audit 阻断 —— **全部到位**。

**无遥测不是缺陷**：对按键模拟工具而言，不做遥测是**正确选择**（Archi 明确判定），本报告不计为缺陷。
- **无 updater：中危，非阻断**（Archi 于 2026-09-19 **主动自我更正**：原判「高」→「低」，本报告采中位并移出阻断清单）。理由是研究稿把 updater 标为 **P2「按需评估」**，且同行并列提及的另外 3 个插件（global-shortcut / dialog / fs）**都已落地**，updater 是唯一未落地项 → 那一行是**评估清单而非承诺清单**。它是**发布成熟度问题**，应与「对外分发复查期」绑定触发，而非挂在阻断位。**真正阻断的是 B3（无 GitHub Release 步骤）**：无 updater 时手动重新下载是唯一升级路径，而这条路径的落点不存在。

**中危项**：
- **ADR 流程从未走完**（见 §4 #3）。
- **一处潜伏跨语言契约断点**：Rust 用 `keys[0]` 作身份键（`hotkey_merger.rs:31-32`），AHK 侧 `SendHotkeyEvent(keys*)` 是可变参，**当前生产未触发**，一旦触发即身份与载荷不一致。这是"IPC 无版本协商/无握手"的具体爆发点 —— 没有版本协商，这类不对称**只能靠"人工记得"来防**。
- **供应链合规无闸门**：有 `cargo audit` / `npm audit`（查 CVE，且阻断），但**无许可证合规扫描**（`cargo-deny` / `license-checker`）。安全与合规是两件事。
- CSP 放行 `'unsafe-inline'`：Archi 定为**可接受**（Tauri 前端是本地打包资源，攻击面远小于 Web），建议并入安全基线 ADR 而非单独立项。

### 5.3 测试与质量门禁 —— 🟢 单测接近生产级 / 🔴 端到端与发布不达标

**门禁阳性对照（本次最硬的证据）**：7 道可自动化闸门**全部实测变红且点名，还原后复跑回绿** —— G1（注入 JS 环 → `js_cycles` 0→1 并列出文件对）、G3f（抬基线/删 lcov 记录各一次，均点名文件与 `min_files`）、G3g（注入失败断言 **+ 注入 `ExitApp(0)` 假绿，两种都红**）、G3d（文档自洽 + 运行时对账两条分支）、G3e（C12 调试端口）。另独立复现 2 组变异对照（`time_format`、前端 `data-action` 接线）均变红。

**两个反直觉结果**：
- **G4 结构上不可能红**（两端都只是打印清单）→ 它不是自动闸门。
- **CI 10 个 job 里只有 4 个真阻断**（gates / js-lint / coverage / security-audit）；`miri`/`fuzz`/`bench` 是 `continue-on-error` **永不阻断**；`ahk-bench` 自称门禁但 PR 上根本不跑（`ci.yml:494`）；`e2e` 与 `release` 结构性不可达。

**覆盖率重取（本机 2026-09-19）**：

| 口径 | 重取值 | 文档登记值 | 差 |
|---|---|---|---|
| 3 个纯逻辑 crate | **91.82%** | 89.05% | 已过期 2.77pp |
| `src-tauri` | **69.98%** | — | — |
| 全仓 | **83.98%** | — | — |

`test-map` 数字与运行时 **666/666 完全一致**。

⚠️ **注意方向**：覆盖率实际值**高于**文档值，所以文档是"过期"而非"虚高"；但正因如此，**棘轮基线存在约 3pp 的静默下降额度**（基线钉在旧值，实际可降 3pp 而不触发告警）。

### 5.4 可运维性 —— 🔴 不达标

**判读：抗崩溃做得好，可排障很差。** watchdog 有 10 次上限 + 退避 + JobObject + panic hook（实测没问题），但**故障信号到不了用户眼前**。

**被推翻的两个假设（方向反了，记下来）**：
- 我怀疑"备份机制只是写进去没人用" → **不成立**，`main.js:727/728/313` 三处都接了 UI。
- 我怀疑"日志按天轮转会无限增长" → **不成立**，`logging.rs:32` 的 `max_log_files(7)` 生效。

**其余中危**：热键注册失败**零日志**（`hotkey_cmd.rs`/`group_cmd.rs` tracing 全空）、AHK 侧**零日志**、无 `ISSUE_TEMPLATE`（实测目录不存在）、`gh` 未登录。

**对既有事故预案的批判**：`full-review-and-incident-response-2026-09-19.md` 的 §4.1/§4.3/§4.5 是 **SaaS 模板**（值班/告警/升级/指标），对单人项目**不成立**，已给逐条增删建议。

### 5.5 文档与发布物料 —— 🔴 不达标

**阻断点全部集中在「用户侧 + 合规」**；工程侧文档（`AGENTS.md` / `developer-guide.md` / ADR 规范）**成熟度反而超出预期**。

**存量文档的两处硬伤**（主理人已本机逐行复核确认）：
1. `developer-guide.md:1681-1684` §4.8 仍写 `asd.log` 与 `Roaming\asd-tauri\` —— **全错**（真实为 `com.asd.tauri` + `asd.YYYY-MM-DD.log`）。这是我上一轮修漏的一处。
2. `developer-guide.md:1600-1601` **代码围栏破损**：1601 是全文第 **105** 个围栏（奇数＝开），下一个围栏在 1634 → **1603–1633 整节被吞为代码块**，含 `### 4.6.6 静态分析档位`、`#### Rust：纯逻辑 crate 开 clippy::pedantic`、其中的表格、`#### 前端：ESLint 9` 三个标题**全部不渲染**。
   （Docu 报的范围是 1601–1636，我复核后收准到 **1603–1633**，结论不变。）

**一致性矛盾 6 处，其中 4 处无任何自动化守护**。

**正面**：15/15 个文档里提到的脚本都真实存在，`bash -n` 与 `py_compile` 全过；日志未进版本库；`docs/adr/README.md` 与 `AGENTS.md`「只写指针不复制数字」是好实践。

---

## 六、✅ 行动清单（分 4 阶段，约 3～5 人日）

| 阶段 | # | 行动 | 对应阻断 | 负责角色 | 紧急度 | 成本 |
|---|---|---|---|---|---|---|
| **阶段 0<br>合规最小集** | 1 | **先做这条**：包内附 GPLv2 许可证文本 + `THIRD-PARTY-NOTICES.md` + `tauri.conf.json` 加 `bundle.license`。**不等待法务意见** —— Q1 是确定的，且**不依赖你选什么许可证** | B2 | Archi | **P0** | 约 1 小时 |
| | 2 | 补 `LICENSE`（需先想清楚与 vendored GPLv2 的兼容，故排在 #1 之后） | B1 | 人类负责人 | **P0** | 0.1 人日 |
| | 3 | 加 `cargo-deny` / `license-checker`（补上供应链**合规**闸门） | — | Cody | P1 | 0.5 人日 |
| **阶段 1 前<br>门禁可信度** | 3b | **补齐 G3a 晋级留痕**：补连续绿跑记录（三套冲突判据取**最严**的一条：本机与 CI **各 ≥10 次**），或把 watchdog 这组单独成步并保留 `continue-on-error`；并**删除已过期的判据段**（`:14-26` 至今仍在文件里，而 `:24` 自己要求摘除时一并删除，属自我违反）。同时**补两道门**：① **G3h 元门禁** —— 断言 `cargo test -- --ignored --list` 为 `0 tests`，去掉 `continue-on-error`；② **用例总数下限门** —— 断言 `cargo test --workspace --all-targets -- --list` **≥ 666 且容差 0**（**针对「测试静默消失」，比元门禁更根本**：它能一并拦住挂 `#[ignore]`、被注释掉、`#[cfg]` 排除、未参与编译、被整个删除五种情形，而元门禁只拦第一种；本项目已有先例 `bridge_tests.rs:178-186` 的 `test_tauri_event_bridge_emit` 是**整个被删除**而非挂 ignore）。⚠️ **基线不可用 Cody 早先提议的 465**（Cody 已自我更正）。**465 的来历值得留痕**，因为它完整暴露了"口径拼错"是怎么发生的：`406` 是**只统计了 3 个 crate、不含 `src-tauri`** 的结果，`59` 是**按名过滤后的子集**（另有 197 条被 `filtered out`），两者相加后被当成了全量 —— 667 − 465 = 202，正好对上被漏掉的那部分。**它与覆盖率那条「量程变了却当成覆盖率提升」属同一类错误**：都是把"局部口径的读数"当成"全局口径的读数"。**主理人决定：下限门纳入本轮 P0，但不独立立项，并入本条 3b**（防的是 TD-058 的 A 类失效，成本仅一条断言、可复用 G3d 采集；独立立项会挤占 12 项真阻断的资源）。基线 666 有**双重来源**（本机 `cargo test --workspace --all-targets -- --list` 实测 + G3d 每次运行时对账输出 161/72/173/3/257），非孤证。⚠️ **基线命令必须先定死**：`--all-targets` = **666**、不加 = **667**、仅 `--lib` = **483**，三者互差 1 与 183，取错命令则基线即错（与覆盖率「必须同一条命令取基数」是同一个坑） | 🔴 发布风险 | Tessa | **P0** | 0.5 人日 |
| **阶段 1<br>可运维止血** | 4 | 诊断页接上 `getExecutorStatus()` 状态条 + `resetWatchdog()` 按钮（**复用现成接口，照抄 `ipc:listener-failed` 模式**） | B4 | Rex + 前端 | **P0** | 0.5 人日 |
| | 5 | setup 期配置加载失败弹窗 + 提示备份恢复入口；保存前自动备份 | B6 | Rex | **P0** | 0.5 人日 |
| | 6 | **实测**看门狗重启后热键是否恢复（结束 `asd_executor.exe` → 按原热键）；若确认不恢复则加重放 | B5 | Tessa | **P0** | 0.5 人日 |
| | 7 | IPC 管道加 DACL（`ListenerOptionsExt::security_descriptor`，**一行代码**） | 高危 | Cody | P1 | 极小 |
| **阶段 2<br>用户侧** | 8 | 补 `README.md`（一句话是什么 / 下载安装 / 3 步上手 / 截图 / 排障入口）。**素材已在库内**：`func_test/01…15` 是成套 UI 截图 | B8 | Docu | **P0** | 0.5 人日 |
| | 9 | 补 `docs/troubleshooting.md`（**纯 GUI 操作路径**，不得再要求调内部 API） | B10 | Docu | **P0** | 0.5 人日 |
| | 10 | 修 `developer-guide.md` §4.8 两行 + 补 1 行围栏（**存量阻断级错误**） | — | Docu | **P0** | 极小 |
| | 11 | 补 `docs/user-guide.md` 或应用内帮助页 | B9 | Docu | P1 | 1 人日 |
| **阶段 3<br>发布链路** | 12 | 手动触发 release job **人工盯全程**；补 GitHub Release 步骤（现在唯一落点是 90 天过期的 artifact） | B3 / B13 | Rex | **P0** | 0.5 人日 |
| | 13 | 跑一次发布产物冒烟（装 → 启 → 执行一个连招），并补进 CI | B12 / B13 | Tessa | **P0** | 1 人日 |

**前置约束（重要）**：
- **阶段 3 必须在阶段 1 之后** —— 否则会产出一个已知缺陷的安装包，反而锁定了一个坏基线。
- **补任何新 ADR 之前，先把 ADR-001/002 转 Accepted** —— 否则只会得到更多"写了但不生效"的文档。
- 覆盖率基线需随本轮提交收紧（消除约 3pp 静默下降额度）。

---

## 七、⚠️ 待完善 / 已知局限

1. **B5（重启后热键不恢复）仍是推断** —— 无重放路径已实测确认，但"重启后确实失效"未经端到端实测。已列为行动 #6，**它决定 B5 是"推断"还是"确认阻断"**。
2. **分支保护未能核实** —— `gh` 未登录，CI 的 branch protection 是否为真阻断是本次**唯一一条纯推断**。建议补一次 `gh api`。
3. **GPL Q2（自研代码是否被传染）无法自证** —— 需专业法务意见。**本报告不是法律意见。**
4. ~~测试计数两个口径未对齐~~ → **已澄清（主理人 2026-09-19 实测，非推断）**：`cargo test --workspace --all-targets -- --list | grep -c ": test"` = **666**，与 `test-map.md:242` 登记值一致（161 + 72 + 173 + 3 + 257）。Cody 报的 465（406 Rust + 59 前端）是其**实跑子集的通过数**，与 666 的「全量注册数」**口径不同、不可直接比较**。实测对照：`--all-targets` **666** / 不加 `--all-targets` **667** / 仅 `--lib` **483** —— **三个命令三个数字**，引用测试数时必须指明命令，否则必然对不上。权威口径以 `test-map` 的 666 为准。
5. **覆盖率数字为 2026-09-19 本机重取**，非 CI 产出；`src-tauri` 是否纳入官方覆盖率口径仍待决策（T1）。
6. **本次评估未做任何代码改动** —— 全部为只读取证。所有修复都是下一步的事。
7. **我自己的派单锚点错了 3 处**（TD-072 性质、CSP 位置、`developer-guide` §4.8 漏修），均由成员纠正并已在此记录。

---

## 八、📚 数据来源 & 成员产出索引

| 成员 | 维度 | 判定 | 原始产出 |
|---|---|---|---|
| Cody（代码审查师） | 代码安全与健壮性 | 🟢 接近生产级（0 阻断 / 2 高危） | `_raw-cody-production-readiness-2026-09-19.md` |
| Archi（架构师） | 架构与分发合规 | 🟡 架构接近生产级 / 🔴 合规不达标 | `_raw-archi-production-readiness-2026-09-19.md`（含第 3 章 GPL 两问拆解、第 6 章 ADR 流程） |
| Rex（SRE） | 可运维性 | 🔴 不达标（4 阻断） | `_raw-rex-production-readiness-2026-09-19.md`（含 Go/No-Go 检查清单） |
| Tessa（测试专家） | 测试与质量门禁 | 🟢 单测接近生产级 / 🔴 端到端与发布不达标 | `_raw-tessa-production-readiness-2026-09-19.md`（含 7 道闸门阳性对照表） |
| Docu（技术文档师） | 文档与发布物料 | 🔴 不达标（5 阻断） | `_raw-docu-production-readiness-2026-09-19.md`（含第 8 章与 Archi 交叉收敛） |
| 主理人（甄宇航） | 编排、去重、取证复核 | — | 本机实测：`target/release/` 零产物、GPL 许可证类型、`developer-guide` §4.8 与围栏（1603–1633）、技术债台账分层统计 |

**交叉复核记录**：Archi ↔ Docu 就 ADR 覆盖度与 GPL 定级做了两轮交叉，收敛结果见两份报告的增补章节。

---

> 本报告由工程保障团队 AI 协作生成。**GPL 相关结论不是法律意见**，Q2（自研代码是否被传染）须由专业法务复核。其余关键决策请由人类工程负责人复核。
