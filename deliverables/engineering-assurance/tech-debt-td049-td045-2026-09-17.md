# TD-049① / TD-045 批次 9 / TD-050 技术债修复报告

**日期**：2026-09-17（收口于 2026-09-18 凌晨）
**工作流**：工作流 5（技术债评估与修复）
**参与成员**：Cody（代码审查师）· Docu（技术文档师）· Docu-2（工程实现/验证）

---

## 📌 TL;DR（执行摘要）

- **完成 3 项**：TD-049①（P1，图谱计数口径 + 重取基线）、TD-045 批次 9（P2，`doc_markdown` 213 项全收干）、TD-050（P2，「反引号跨中文」分类后修复 5 处）。
- **遗留 2 项待排期**：TD-051（图谱 JS 侧盲区，P1）、TD-052（时延断言在宿主负载下偶发红，P1）。
- **严重度分布**：🔴 严重 0 · 🟠 高 0 · 🟡 中 3（已完成）· 🔵 待排期 2（均 P1）。
- **阻塞 / 非阻塞**：**非阻塞** —— 全量门禁 9 项全绿（G1/G2a/G2b/G3a/G3b/G3c/G3d/G3e/G3g），仅 G4 需人工确认。
- ⚠️ **本轮最重要的发现**：**`clippy --fix` 处理文档类 lint 会制造「假绿」** —— 被反引号包住的中文不再被 lint 检出，因此 TD-012「248 项收干到 0」里含有假绿。

---

## 🎯 核心结论卡片

| 项目 | 内容 |
|------|------|
| 整体评级 | 🟢 通过 |
| 阻塞项数量 | 0 |
| 关键行动项 | 3 条 |
| 提交 | `6bbaaa1`（TD-049）、`fa9caa2`（TD-045）、`83085a0`（台账）、`9e106c9`+`7da49a8`+`1bc3672`（TD-050） |
| 建议下一步 | 排期 TD-051 / TD-052；分支已领先 `origin/main` 约 165 个提交，需确认推送策略 |

---

## 一、TD-049① —— 图谱计数口径（P1，已完成）

### 台账原描述失准，真实缺陷是另一条

原描述称「`EXCLUDE_DIRS` 只作用于边、不作用于节点；同一 cohort 混了 17 个生产文件与 1 个 `tests/lib/` 文件」。复核结果：**三句全部站不住** —— `tests/lib/` 在当前仓库根本不存在；`EXCLUDE_DIRS` 本来就在节点发现阶段（`git_scope_files` L73）生效。

真实缺陷（更严重、台账未记）：

> `git_scope_files` 用 `git ls-files --cached --others --exclude-standard` 一把梭，`--others` 出来的未跟踪文件**只存在于开发机**、CI 全新 checkout 上为零。
> **实测：开发机 `.rs` 节点 61 个 vs CI 口径 57 个，开发机 `rust_files` 比 CI 多 4 个。**
> 基线里「本基线在开发机与 CI 全新 checkout 上应当一致」这句承诺**从一开始就是假的** —— **TD-025 只修掉了一半**（去掉被 ignore 的，留下未跟踪的）。

### 修法与新增守护

| 项 | 内容 |
|---|---|
| 口径 | 节点发现只认 `--cached`（与 C6 同口径）；未跟踪文件隔离到 `*_files_untracked`，不进任何对比项，由闸门硬失败 |
| 分桶 | AHK 切 `prod` 40 / `test` 23 / `tool` 29，互不相交；`ahk_unclassified_files` 恒 0 |
| 新硬失败 | `ahk_prod_to_nonprod_edges`（生产 `#Include` 测试/工具 = 架构倒灌）、`ahk_orphans_prod`、分桶口径完整性、6 个遍历锚点 |
| 孤点误报 | `key_receiver.ahk` 经跨语言引用（`key_receiver.js:18`）洗清，`ahk_orphans` 6 → 0；扫描带 3 条防放水约束 |
| 阳性对照 | 7 组，含「节点发现压到 0 → 报 7 条失败而非绿灯」、「两侧协同篡改被闸门内**独立实现**的分类器抓住」 |

**基线重取**（未用 `--update-baseline` 全量刷新）：`ahk_files` 97→92、`ahk_edges_total` 285→287、`ahk_orphans` 6→0、`rust_edges` 163→165、`js_files` 4→5、`js_edges` 6→11。原 7 项结构变化 WARNING 全部消失。

---

## 二、TD-045 批次 9 —— `doc_markdown` 213 项（P2，已完成）

### 数量订正

两位成员**独立实跑**都得 **213 项 / 12 个文件**（非台账旧记的 211 —— 差额来自批次 8 补 `# Errors`/`# Panics` 又带进 2 项）。

### ⚠️ 核心结论：`cargo clippy --fix` 对本条不可用

clippy 不认 CJK 词边界。实测 **213 条建议里 21 条（9.9%）`suggested_replacement` 本身就是坏的**：

| 位置 | clippy 的错误建议 | 正解 |
|---|---|---|
| `bridge.rs:182` | `` `中的锁顺序（ipc_manager` `` | 只包 `ipc_manager` |
| `ipc.rs:635` | `` `try_send，通道满时丢弃而非` `` | 只包 `try_send` |
| `system_cmd.rs:44` | `` `子进程可能已重启（post_connect_callback` `` | 只包 `post_connect_callback` |

**判据**：从建议区域取 **ASCII 标识符运行段 + 前后边界校验**，中文一律留在外面；含中文的逐条人工判定。

### 防复发三层

1. **既有闸门 G2b**（`-D warnings`）
2. **新增 `src-tauri/src/tests/doc_markdown_contract_tests.rs`** —— 两条静态扫描（源码级 blanket `#![allow(` + `Cargo.toml` 的 `[lints.clippy]`），**只禁 `#![allow(` 不禁 `#[allow(`**（后者是「就地 allow + 代码内理由」的合法口子，禁掉等于逼人写假文档），两条都带防永真式锚点
3. **`tools/check_added_backticks_cjk.py`** —— 只比对 HEAD 与工作区、只看**本次新增**的反引号对，避免误报 `` `"无效的文件路径"` `` 这类正确写法

**零就地 allow 例外**：213 项全部真修。实测新增反引号对全部只含 ASCII。

---

## 三、TD-050 —— 历史「反引号跨中文」（P2，已完成）

### 先量化再动手：29 处里真正该修的只有 5 处

用「反引号内含 CJK **但内容不是字符串字面量**」的判据扫 58 个 tracked `.rs`：**29 处命中** → 14 处字符串字面量（合法，中文是字面量内容，如 `` `"无效的文件路径"` ``）+ 15 处疑似 → 逐条分类：

**A 类 5 处真损坏（已修）**，共同特征是**反引号边界落在中文散文中间**：

| 位置 | 原文 | 修法 |
|---|---|---|
| `asd-application/src/group_service.rs:442` | `` `config_state）和` `` | `）` 与连词移出反引号 |
| `asd-application/src/state.rs:506` | `` `Ok(Some(占用的 group_id))` `` | 用通配符 `_` 占位把散文移出表达式 |
| `asd-application/tests/common/mod.rs:4` | 反引号在 `（` **之前**断开 | 改为在 `（` 之后开始 |
| `asd-application/tests/common/mod.rs:5` | 反引号在 `）` **之后**断开 | 改为在 `）` 之前结束 |
| `asd-application/tests/recording_service_tests.rs:121` | 整句中文 + 尾部标识符被一起包住 | 中文移出，只包 `recording_mode` |

**B 类 10 处刻意保留、不许改** —— 中文是名称的一部分，改了反而错：
- `system_cmd.rs:207/208/320/339/359`：**任务清单条目名的整体引用**（`` `emergency_release 在无效状态下失败` ``）
- `ipc.rs:499`、`` lib.rs:643 ``、`command_contract_tests.rs:142`：代码表达式 / 元组字段描述
- `config_repository.rs:71`、`config_cmd.rs:91`：**文件名模板**（`` `backup_<时间戳>.json` ``）

### 守护设计的关键取舍

新增 `crates/asd-application/tests/backtick_cjk_contract_tests.rs`：先遮蔽 `"..."` 字面量与 `<...>` 占位符，再判 CJK 与 ASCII 标识符是否粘连。

⚠️ **B 类不许用「加豁免」的方式过关** —— 判据误报就改判据。落地方式是另加两条**判据回归测试**，把 A 类 5 个原样本与 B 类 8 种形态**锁死在测试里**（防判据被改松、也防判据过粗），而不是在扫描器里开后门。

**阳性对照实测**：把 `state.rs:506` 改回损坏 → 精确报出文件:行；注入一处「反引号包住中文散文」→ 报出；两次注入在 58 文件全量扫描里**都只命中注入那一处**，B 类 10 处一次都没被误报；还原后 3 条测试全绿。

---

## 四、遗留待排期

| ID | 档 | 内容 | 为什么值得记 |
|---|---|---|---|
| **TD-051** | P1 | 图谱 JS 侧盲区：`asd-tauri/e2e/` 被 `extra_exclude` 整体排除，e2e 的 JS 互相 import 且 spawn AHK | JS 是**唯一没有环检测**的语言；且改坏 `extra_exclude` 无人察觉（去掉尾斜杠就永远匹配不上，已踩过两次） |
| **TD-052** | P1 | `Test_ExecuteHybrid_SubGroupsKeepTheirOwnRhythm` 时延断言在宿主负载下偶发红（P95 21.54ms > 上限 20.0ms；单独重跑 722 全绿） | 用例注释**自己就记录过**这个风险，已知未根治；偶发红会阻断合并，而「重跑即绿」会训练人看到红先点重跑 |

---

## ✅ 行动清单

| # | 行动 | 负责角色 | 紧急度 | 预期完成 |
|---|------|---------|--------|---------|
| 1 | 排期 TD-051：给 `extra_exclude` 加守护，并决定 JS 侧是否补环检测 | 代码审查师 | P1 | 下一轮 |
| 2 | 排期 TD-052：给时延类断言单独的重试/降噪策略，**不放宽上限、不跳过用例** | 测试专家 | P1 | 下一轮 |
| 3 | 决定推送策略：分支已领先 `origin/main` 约 165 个提交 | 工程负责人 | P1 | 尽快 |

---

## ⚠️ 待完善 / 已知局限

- **G4（文档同步）为人工核对项**，本报告无法自动确认 6 条清单是否都打勾。
- **未推送**：本轮 6 个提交仅在本地，未推远端、未查 CI 结论。
- **TD-050 曾登记不准**：最初写的「11 处（本 crate 7 + asd-application 4）」是粗扫结果，**既混进合法写法又漏了真损坏**，已作废并改写为 A/B 分类。教训：**两份成员清单对不上时不能各采信一半，必须自己拿判据重扫**。
- **TD-045 有 3 处「清单外补充」**（`lib.rs` 的 `watchdog` 等，clippy 不会报的小写单词）：语义正确且 CJK 安全，决定保留并记入台账，不回退。

---

## 📚 数据来源 & 成员产出索引

- **Cody（代码审查师）**：TD-049① 缺陷定位与修复、7 组阳性对照、基线重取。产出见 `.review-analysis/build_graph.py`、`scripts/check-graph-baseline.py`、`graph-baseline.json`。
- **Docu（技术文档师）**：TD-045 实现（21 条含中文项人工判定）、`tools/check_added_backticks_cjk.py`、TD-050 的 A 类修复与守护。
- **Docu-2（工程实现/验证）**：213 项清单采集与分桶统计、影子 diff + 新鲜度校验机制、`doc_markdown_contract_tests.rs` 守护、4 组阳性对照。
- **主理人独立复核**（未采信成员自述）：全仓 `N / N` 形态扫描确认 C3b 无新增；`cargo fmt --all --check` rc=0；TD-050 修复后 A 类清零、B 类 10 处保留；全量门禁 9 项全绿（AHK 722/722）。

---

> 本报告由工程保障团队 AI 协作生成，关键决策请由人类工程负责人复核。
